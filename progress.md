# kimi-base 项目记忆

格式约定：Pinned（受保护，不可自动修订）/ Decisions（只追加）/ TODO / In Progress / Done（带证据指针）/ Risks & Assumptions / Notes。>100 条归档到 progress.archive.md（只增不删）。

## Pinned（必守铁律）

1. 证据优先：DONE、Bash 成功、validate 结构通过都不算质量通过；只有绑定当前 git fingerprint 的 gate receipt 算。
2. 绝不假绿：缺工具/缺命令/非 git 仓 = BLOCKED 或可见降级；SKIP 必须显式。
3. security、safety 与 privacy 永不豁免、永不 fast-skip；waiver 只豁免"跑不了"，不豁免"跑挂了"。
4. 治理退出必须留痕：none/minimal 定档要书面理由，waiver 要五要素，fast 要 TTL。
5. 不写第二套 Agent runtime；不修改 Kimi Code 内核；hooks 是护栏不是沙箱（fail-open 如实写文档）。
6. 非 kimi-base 项目（无 .kimi-base/harness.json）中一切 hook 与 sessionStart skill 必须静默。
7. 测试独立性：写测者 ≠ 被测作者；测试不许为迁就实现而改断言。

## Decisions（只追加，含被否方案与理由）

- 2026-09-10（P12 REQ-080/081）机械闸三决策：① 清脏只在终审 ACCEPT（verdict==='ACCEPT' && final）。否决「任意 ACCEPT 即清」：非终审 ACCEPT 仅阶段通过，清脏会让 Stop 在终审前放行，闸形同虚设。② 脏标记生效面 = 置脏集 ∩ 当前 git 变更集（Stop 时 prune 落盘）。否决「置脏即永拦」：已还原/已提交的文件会让陈旧标记永久误拦，熔断也救不了新清单。③ --scores/--evidence 走 CONTRACT_FLAG_EXTENSIONS 单列导出。否决「直接入 CONTRACTS.feedback.flags」：tests/cli-contracts.test.mjs 把 40 verb 的 flag 名集逐键锁定（含不多列对账），入表即红且测试不可改——循 REQ-063 catalog --level 先例，扩表转正待测试作者。
- 2026-09-08（P8 修复轮二）dod 同 id 去重的调和语义：同 id 且同命令面（argv 一致）= 同一检查，跳过执行；同 id 不同命令面 = 不同测量，两边都跑，同结果 PASS 折叠显示（collapsed note 披露）、异结果响亮单列。否决「纯按 id 跳过」（终审 W3：同名必败命令会被误吃掉，dod 假绿）；否决「异命令面也各占一步骤行」（修复轮一既有用例锁定同 id 步骤行只出现一次）。执行层不重不漏，显示层不重复确认——折叠的只是同一结论的重复确认，失败永不折叠。

- 2026-09-08（P8）tier 必填的执法点定在 gate --dry-run 配置校验面与 dod 分组（缺 tier 保守归 middle 并响亮提示），而非 validateMatrix 一刀切必填。否决「validateMatrix 无条件必填」：存量绿测（audit.test.mjs 的 gate 全量跑、adr check 夹具、scale releaseFixture）均以 tierless 矩阵喂共享校验入口，一刀切会让既有绿测全红而测试不可改；dry-run 面执法保证新矩阵从第一天起标层，存量执行面平滑接入。非法 tier 值仍在 validateMatrix 无条件拒绝（无任何存量面依赖非法值）。
- 2026-09-08（P8）`catalog discover --level` flag 经 cli-contracts.mjs 的 CONTRACT_FLAG_EXTENSIONS 单列导出接线，而非直接扩 CONTRACTS.catalog.flags。否决「直接入表」：tests/cli-contracts.test.mjs 把 40 verb 的 flag 名集逐键锁定（含「不多列」集合对账），入表即红且测试不可改——循 STRENGTH_CONTRACT 先例扩表先行、转正待测试作者；未知 flag 报文的「支持的 flag」清单仍按契约基表列（锁表对齐），扩表 flag 在 catalog --help 细则披露。

- 2026-08-13 分发形态定为「Kimi 插件（hooks+命令+路由 skill）+ 项目模板注入」双层。否决「纯项目注入」：Kimi 无项目级 hooks，纯注入失去机械闸门；否决「纯插件」：插件用户级生效会污染非治理项目，且项目资产（AGENTS.md/catalog）本就该随仓走。
- 2026-08-13 项目资产放 `.kimi-code/`（agents/skills）而非 `.agents/`：`.agents/` 是跨工具共享目录，避免与 codex-base 等同仓安装互相覆盖；Kimi 原生优先扫描 `.kimi-code/`。
- 2026-08-13 项目配置收敛到 `.kimi-base/harness.json` 单一配置源；catalog/matrix/adapters 独立文件（大仓 catalog 可能很大）。
- 2026-08-13 五性采用用户指定五属性（resilience/security/safety/privacy/reliability）为治理核心，扩展属性（availability/performance/maintainability）允许在 catalog 声明但非默认要求。否决八属性全集默认强制：认知负担大于收益（与 codex-base 拒绝理由一致）。
- 2026-08-13 多模型异构审查不移植（Kimi 单宿主），以红蓝对抗+多视角 lens+写测独立补偿，记录为已知弱化项。
- 2026-08-13 runtime 单文件零依赖（家族共识形态），supervisor 独立文件。
- 2026-08-13 hook 调度以 payload.cwd 定项目根（插件 hook 进程 cwd=插件根，不可依赖 process.cwd）。
- 2026-09-02 v2.0 重构启动（对标并超越 dsh-base，后者已逐行精读）。源布局=安装布局自托管（学 dsh 单目录 `.dsh/`；否决维持 template/ 双份：复制面与自用治理分离必然漂移，dsh 证明单布局可行）。种子语义：三配置+AGENTS.md 只在目标缺失时落地、upgrade 永不覆盖、uninstall 仅删未改动者（dsh 模式）。fleet 完整移植（用户拍板）。privacy 入保护底线：protected={security,safety,privacy}（用户拍板，修订 2026-08-13 决策的保护子集；五性治理核心不变）。
- 2026-09-02 doctor/manifest 的源仓判定优先于 installed 判定：自托管后本仓根同时存在 harness.json 与 kimi.plugin.json，installed 优先会让 `manifest --check` 在源仓误报 NOT_INSTALLED；已安装目标绝无 kimi.plugin.json（不发布），无误判风险。
- 2026-09-02（P4）rules-audit 阈值段定名 `rulesAudit.maxUnenforced`：否决 dsh 式 `rules.maxUnenforced`——harness.json 的 `rules` 已被 rules 文件指针数组占用，严格校验器拒绝同名异型。
- 2026-09-02（P4）trace 扫描面 = tracked ∪ 未跟踪（exclude-standard）：否决 dsh 纯 tracked——追溯是工作树事实门，新测试文件未提交时其引用必须立即可见，否则覆盖率在开发期撒谎、提交后才变绿。
- 2026-09-02（P4）PLACEHOLDER 的 ASCII 词（TBD/TODO）按独立成词判定（两侧为字母数字/'/'/引号书名号不算）：段名枚举不是占位符；中文占位词保持子串判定。
- 2026-09-02（P4）recap/invariants 的截断注记计入预算，总量绝不突破：否决 dsh 截断后再追加注记导致输出超预算的形态。
- 2026-09-02（P4）需求 id 同时接受裸形式与领域形式（REQ-001 / REQ-<域>-001）：kimi 文档两种都用；对称规则保留——只为声明过的 id 族扫描引用（dsh 扫 HAZ/THR 却从不声明，dangling 必然误报，不搬）。
- 2026-09-02（P5）pre-push 的 gate 不引入 dsh 式 `--baseline <upstream>`：kimi 的 gate 回执绑定整棵树内容指纹，任何字节漂移都使旧证据 stale 并 fail-closed，范围语义已被覆盖；加一个 gate 不认识的 flag 只会让 push 恒红。否决逐字移植 dsh pre-push 形态。
- 2026-09-02（P5）`fitness --all`（dod 全仓步）扫描口径 = tracked ∪ 未跟踪（exclude-standard），与 trace 同口径：否决纯 tracked——新文件未提交时扫描面撒谎。`--staged`（pre-commit 用）则只看暂存区，范围语义诚实。
- 2026-09-02（P5）审计脚本（.kimi-base/audit/）与引擎刻意重复实现（manifest 哈希面/机密模式表各写一份）：审计者与被审计者独立，引擎缺陷无法让审计沉默；重复是职责不是 Bug（dsh ADR-0002 同款）。
- 2026-09-02（P6）Pinned 第 3 条修订为 protected={security,safety,privacy}：用户拍板的宪法修订（非"自动修订"），与 AGENTS.md 证据法第 3 条拉齐；全 payload/docs 的"security/safety"旧表述同批清扫（rules×3、templates×2、skills×3、plugin/commands、docs×3、hooks.mjs invariants）。
- 2026-09-02（P6）discover 分层方向相对 dsh 翻转：kimi 层规则是「只许依赖同层或更内层（层索引更小）」，dsh 的 "tier-1 outermost" 命名在 kimi 规则下会让草案自带 layer-direction 违规——故 tier-1 = 最内层（无依赖基础层），层号沿最长依赖路径递增。
- 2026-09-02（P6）discover 模块 id 只剥**一个**前导源码根段：dsh 剥所有段，恰好叫 app/lib 的模块会被抹成 `packages-app` 回退 id（供体怪癖，实测复现后修正）；分组前剔除 ignore 候选路径（dsh 会把 docs/ 同时放进模块提案与 ignored，草案自相矛盾）。
- 2026-09-02（P6）fleet status/recap 不移植 dsh 的 stdout 单行 JSON 解析（kimi 引擎输出是中文人类文本）：改用退出码 + 文本行解析；新增 `KIMI_BASE_ROOT` 环境变量钉死项目根（findProjectRoot），防成员仓嵌套时向上查找串到祖先仓——dsh 有 DSB_ROOT 同款，kimi 此前没有。
- 2026-09-02（P6）waiver 禁词面扩展到 waiver 自身的 reason/compensation 文本（任务书：含 privacy/PII/隐私/个人信 词即拒）；matrix 的 allowFastSkip 保护判定维持 kimi 既有双通道（kind 或认领属性任一命中即拒）——dsh 的 class-vs-attributes 盲区在 kimi 结构上本就不存在，privacy 入列即闭合。
- 2026-09-02（P6）budget 未配置 = exit 3 降级（不假绿），否决 dsh 式内置默认值（dsh catalog.budget 恒有默认 40/1500/3/25）：没声明的预算不是预算。release 的 review-backlog 过期条目定为**阻断**（dsh 是非阻断建议项；任务书将 no-expired 列入 blocking）。
- 2026-09-02（P7b）fast 语义定案「借账不是折扣」：带 fastWindow 印记的 SKIPPED 永远不能关闭 task（completionGate 记缺口并指向还债路径），与 release 的 fast-debt-repaid 同规则拉齐。**否决 kimi v1 窗口内 complete 语义**（窗口内 SKIPPED 计入完成门、债只在 release 拦截）：dogfood 实测它让 task 在证据延期状态下关闭（exit 0），债对 task 层不可见，两层两套规则必生歧义。还债路径唯一：fast off 后重跑完整 gate。有效 waiver 覆盖 BLOCKED 不变。
- 2026-09-02（P7b）dod 与 release 职责划分定案：**完整性归 dod，新鲜度归 release**。dod 的 receipt-verify 步把 exit 4（stale-only）归级为新状态 STALE（区别于 PASS/FAIL/DEGRADED），响亮可见但不阻断 dod 判定；release 的 ledger-intact 只判完整性问题（篡改/断链/缺失/漂移，与其注释一致），staleness 由 receipt-fresh 判定。否决「stale 按 FAIL 计」的旧归级：提交已评审工作后回执必然 stale（正常流程痕迹），旧归级使干净全门禁仓永远 NOT READY。range 评审回执（review start --base，P3）绑定 range.head：HEAD 未移动即 fresh（verify 不判 stale、release receipt-fresh 接受），HEAD 移动才 stale——提交后评审走 range 模式。
- 2026-09-03（#8 验证）UNSAFE_TARGET 区分器 = 源根是否存在 kimi.plugin.json：已安装项目用自带引擎对自身重跑 install/upgrade（含 `install . --hooks`——doctor 自印的补救路径）必须允许；否决旧判据「目标与引擎根重叠即拒」（它把已安装项目的自我升级变成死路）。脚手架源仓自我安装仍拒。
- 2026-09-03（#8 验证）三个 example 种子源纳入受管载荷（恒等映射，dsh 同款：catalog.example.json 随 .dsh/base/ 分发）：否决「种子源不发布」——已安装项目重跑 install 时复制面校验需要它们在场，否则 SOURCE_SURFACE_MISSING。audit/manifest.mjs 独立实现同步对齐（独立性测试当场抓到两侧 digest 分歧，正是该测试的存在意义）。
- 2026-09-03（#8 验证）githooks/ 与 skill-builder scripts/ 内置目录级 `.gitattributes`（`* text=auto eol=lf`）：Windows autocrlf 检出 CRLF 会让 sh 钩子语法报错；目录级文件不侵入采纳者根配置。
- 2026-09-04（v3.0 立项）三仓逐行对标（dsh/cc/codex）+ 业界调研后用户拍板 v3.0 三方向：规划+逐 Phase 推进；档化模型=强度策略引擎（借鉴 codex v5 Assurance 改良：四档×12 封闭轴、extends 只收紧、floor 只升不降、policyHash 绑证据、shadow 迁移、decision log）；feedback 四层进化引擎化。否决：简化档化（无 floor 无 policyHash）、OPA/Rego（零依赖铁律）、多模型审查（单宿主）、sh/ps1 双写（Node 单实现）。
- 2026-09-04（v3.0 P1）需求生命周期标记 planned：规格先行与 trace 100% 覆盖的缝合机制——planned 不计入覆盖率分母但仍受全部可判定性 lint，实现落地同 commit 摘标记由 PLANNED_HAS_TESTS 警告看护。否决：规格分批立项（spec 永远描述过去）、空测试文件占位（trace 只数引用会谎报绿）、planned 跳过 lint（烂需求庇护所）。块判定在下一 REQ id 处截断，防跨条目标记渗漏。
- 2026-09-04（v3.0 P1）trace 输出末行增机器可读投影 `trace 摘要：{...}`（承"操作统计 JSON"先例）：行为测试需要 JSON 字段锚点，且 v3 方向是全动词机器可读投影。
- 2026-09-08（P11 立项）用户指令把框架定位从"工程治理"扩展为"人与 AI 共同理解问题、形成判断、完成交付的沟通桥梁"——软层与工程面同等强大。软层审计发现七大断点（认知无标注/业务含义不过链/交互深度一刀切/迭代不采动机/界面阶段缺失/记忆无依据链/无反例回流），全文取证在 docs/COMMUNICATION-LAYER.md，决策在 ADR-0012。否决：全套 SDD 流水线用于小改动（review overload 实证）、BMAD 角色剧场（无对照证据）、认知标注靠模型自觉（knowing-but-not-showing 实证必须模板强制）。认知标签定为四态 `[确认]/[推断]/[建议]/[未知]`（不用"待定"——撞 spec lint 占位符词表）。

## TODO

- [P2][DONE][#13] P0 校准发现的 discover 三缺陷 + fitness 重复报告已由 REQ-068 修复并关闭（见 Done 2026-09-04 REQ-068 条目与追记：6/6 行为测试 + 真实仓 discover→lint→arch 全链路走通，lint overlap=0，arch 0 违规 0 存量债）
- [P3][OPEN][#14] 插件 E2E 发现 F2：SessionStart hook 的 stdout 横幅在 `kimi -p` 模式下不进模型上下文（阻断型 exit 2 stderr 能到达）；F3：仅标记未 setup 项目首次体验摩擦（skill 找不到项目内引擎）。v3.0 P8 压缩边界设计时按此宿主现实降级设计并写入文档

## In Progress

- [v3.0] 重构进行中（P0-P4 已完成并提交；P11 沟通桥梁层立项完成，正在实施：REQ-069~075）。计划锚点：DEV-PLAN.md v3.0 Phase 表；会话恢复先跑 `recap` 与 `invariants`。

## Done

- v3.0 P12 最终修复轮：trust lens 四 warning 清算（2026-09-10；主代理消息中 4 问题清单缺失，本轮问题集自 .kimi-base/state/review/session.json 的 trust 阶段 lens 报告提取——4 个 warning 级实现面问题恰好对应三 lens 交叉点名的四处；testing lens 另两条 warning 指向 tests/hooks-ext.test.mjs（恒真断言/三振变异幸存），属测试作者面未动；红测未到，先实现并逐项故障注入实证）：① hook 超时 vs 锁超时错配（security/reliability/resilience 同点）：宿主 post-edit 5s/stop 10s < 锁等 15s，持锁时引擎被宿主强杀、降级留痕路径不可达——hooks.mjs dispatchHook 把 ctx.locks.timeoutMs 封顶 3s（LOCK_TIMEOUT 降级留痕先于宿主强杀），kimi.plugin.json post-edit timeout 5→10 留余量；实证：活进程持 review-dirty.json.lock 后 post-edit 3.1s 走 LOCK_TIMEOUT 留痕（修复前实测 5s 被 timeout 杀死零留痕）。② plan-lint 未钉仓根（privacy/reliability/resilience 同点 plan-lint.mjs:47）：git rev-parse --show-toplevel 钉根（兼作 git 仓判定，替掉原 ls-files 探针），DEV-PLAN.md/progress.md 均从仓根读，输出 JSON 增 scanRoot；实证：子目录运行 lint 到仓根计划（含 TODO → exit 1 正确命中），不再假绿/假降级。③ ui-slop symlink 越界读（resilience/security）：lstatSync 探 symlink + realpath 校验出仓即跳过，skippedSymlinks 计数进 JSON 与 stderr 留痕；实证：仓内 symlink 指向仓外含密钥样式注释的 css 被跳过，excerpt 零外泄。④ review.mjs 终审 ACCEPT 清脏静默 catch（privacy/reliability/resilience 同点 review.mjs:467）：改 stderr 警告 + gate-log 留痕（review/dirty-clear-failed，decision=warn），裁决不拖死（方向安全：残余脏标记 Stop 继续拦）；实证：review-dirty.json 被目录占用时 ACCEPT 照出（exit 0）+ stderr 警告 + gate-log 条目，可排障。证据（无管道取码，2026-09-10 本机亲跑）：node --test tests/audit.test.mjs+hooks-ext.test.mjs+review.test.mjs+harness.test.mjs 157/157（manifest 重生成后）；run-tests.mjs 16 文件全绿 exit 0；npm run manifest 重生成（115 文件，digest 0019d2622687）后 manifest --check exit 0；arch check（review→hygiene 新边合规）/sync-check/selftest（27/27）/gate（PASS=4）全 exit 0；dod exit 0（PASS=13 FAIL=0，STALE=1 既有 receipt 陈旧债）。

- v3.0 P12 修复轮：评审 3 warning 清算（2026-09-10；红测由并行测试作者补落于 tests/audit.test.mjs——子目录钉根/悬空编号两例红测到达时实现已按任务描述先行落地，发现口径分歧后改从测试契约）：① plan-lint 编号豁免悬空（W1）——首版按任务描述实现「全文 #\\d+ ≥2 次」口径，测试作者红测落地后改从其契约：定义面 = progress.md 唯一事实源（被引 #N 须在其中出现，含 [#N] 形态与 Done 历史提及；无 progress.md 时规则不激活，宁可漏报不误报），DEV-PLAN.md 首版编辑回滚（测试注释以 DEV-PLAN.md:95 为真仓锚点）。② ui-slop 子目录假绿（W2）——`git rev-parse --show-toplevel` 钉根后 ls-files 扫全仓、文件读取拼 scanRoot、输出 JSON 带 scanRoot 字段 + stderr 标注「扫描面=仓根」，子目录运行不再缩小扫描面报 ok:true。③ 脏闸 catch 静默（W3）——hookPostEdit 置脏失败与 hookStop 剪枝失败均改为 stderr 一行 + gate-log 留痕（hook:post-edit dirty-mark-failed / hook:stop dirty-prune-failed，decision=warn，best-effort 嵌套 catch）后按无脏继续，同 hookPreWrite 降级留痕先例；/tmp 夹具实证：state 路径被文件占用时 post-edit exit 0 + stderr 警告；review-dirty.json 被目录占用时 stop 留痕 dirty-prune-failed 且完成门其余检查照常 exit 2。已知边界如实记录：hookStop 的 session.json 读取在 state 路径整体不可用时会 ENOTDIR 抛错 exit 1（评审前既有行为，非本轮范围）。证据（无管道取码，2026-09-10 本机亲跑）：node --test tests/audit.test.mjs 49/49、tests/hooks-ext.test.mjs 10/10；run-tests.mjs 16 文件全绿 exit 0；npm run manifest 重生成（115 文件，digest 06fb2bdfeba9）后 manifest --check exit 0；selftest 27/27 exit 0；gate PASS=4 exit 0；dod exit 0（PASS=13 FAIL=0，STALE=1 receipt-verify pack-check 回执为既有 strength policyHash 陈旧债，按设计不阻断）。

- v3.0 P12 REQ-079 计划无占位符机械检查（2026-09-10；红测先行兑现：tests/audit.test.mjs 纯追加「plan-lint」7 例起步 7 红——红因核对为特性缺失本身（plan-lint.mjs 不存在，runAudit ENOENT 启动失败），实现未动任何既有断言）：① 新增独立审计脚本 .kimi-base/audit/plan-lint.mjs（零依赖、禁 import 引擎、stdout 单行 JSON + stderr 人类摘要；exit 0 干净 / 1 有命中带 file:line / 3 降级：非 git 仓 reason=not-a-git-repo 拒绝猜测、无 DEV-PLAN.md reason=no-dev-plan 响亮降级绝不假绿）。规则面：占位词表与 spec lint 同源（TBD/TODO 独立成词——两侧为 \w/书名号/引号即视为枚举名不算占位；待补充/待定裸词）+ 偷懒引用（`类似\s*Task`/`同\s*Task`/「同上任务」）；豁免口径写进脚本头注释：围栏代码块（闭 fence 同字符且长度 ≥ 开，口径同 spec lint）一律豁免；行内注释（同行 `<!-- -->` 区段剔除、`//` 前字符非 `:` 起至行尾不参配——`:` 例外防误伤 URL）豁免；`TODO #N` 式编号条目交叉引用（命中词后紧跟 `#<数字>`）不算占位——本仓 DEV-PLAN.md:95「TODO #7 真实仓性能校准」即此形态，是已完成条目对编号事项的引用而非没写完，与 spec lint 独立成词豁免同源，特此留痕（若主代理选择改 DEV-PLAN 行文，本豁免可拆除收紧）。② CI 接线三面：本仓 .github/workflows/ci.yml（ui-slop 步后插计划占位符检查步，dogfood）+ templates/github-gate.yml（planlint 步 blocking 入汇总判定）+ templates/gitlab-gate.yml（step 计划占位符检查 + 头注释管线描述同步）；docs/OPERATIONS.md §10 六脚本→七脚本 + 表行（含豁免口径）+ CI 链描述同步（顺手补上此前漏写的 run-eval 环节）。③ planned 标记按分工不动（REQ-079 还有 arch-designer/dfx-designer/dev-planner 三 skill 锚点面，摘标记归主代理）。证据（退出码均无管道直跑，2026-09-10 本机亲跑）：node --test --test-name-pattern="plan-lint" tests/audit.test.mjs 7/7（占位词 3 形态带行号 3/4/5、偷懒引用三形态带行号 2/3/4、干净计划绿、无文件 exit 3 reason=no-dev-plan 且 ok:false、非 git 仓 exit 3、围栏+行内注释+TODO #N 豁免绿、真仓 DEV-PLAN.md exit 0）；plan-lint 真仓直跑 exit 0（DEV-PLAN.md 119 行 0 命中）；npm run manifest 重生成（115 文件，digest 547afc715120）后独立 manifest --check exit 0；run-tests.mjs 16 文件 452/452 exit 0；selftest 27/27 exit 0；gate PASS=4 exit 0 → dod exit 0。过程事故如实记录：恢复他人未提交工作时误用 Write 覆盖模式 + git checkout 一度冲掉 tests/audit.test.mjs 工作树版本（含 REQ-078 ui-slop 未提交测试块），已从会话内完整 Read 记录逐字重建并经 ui-slop 8/8 与全量 452/452 验证无损——教训：脏工作树禁用 git checkout 恢复，追加一律 Edit。

- v3.0 P12 REQ-080 review 机械闸 + REQ-081 进化补强（2026-09-10；红测先行兑现：新文件 tests/hooks-ext.test.mjs 10 例起步 6 红——红因核对为特性缺失本身：hook post-edit 未知事件 exit 1（置脏两例/清脏）/ Stop 阻断原因无评审指引 / prompt-submit 提示未点名 feedback-observer / --scores 未注册 flag；另修两处假绿断言——evidence 缺维度/越界例原以「未知 flag」误绿，补 doesNotMatch(/未知 flag/) 区分红因；实现未动任何既有断言）：① REQ-080 机械闸——新增 lib/review-dirty.mjs（脏标记状态单源：代码扩展名白名单判定 isCodePath / 仓内归一 dirtyCandidate（仓外·.git·.kimi-base 一律不置脏）/ markDirty·pruneDirty·clearDirtyForPaths，仅依赖 core/paths/state 防循环）；hooks.mjs 新增 post-edit 事件（PostToolUse Edit/Write，观察型——宿主忽略返回值故永不阻断、stdout 不进模型上下文，两坑设计内避开）；hookStop 增脏闸（脏标记 ∩ 当前变更集 = 未评审代码改动 → exit 2 指引派发 review；已提交/已还原文件剪枝落盘防陈旧标记永久误拦；阻断清单进既有保险丝键 sha256(指纹+problems)，同一清单连拦 stopMaxBlocks 次后第 4 次放行 + stop-fuse-release 欠账留痕）；review.mjs 终审 ACCEPT（verdict==='ACCEPT' && final）按会话范围清脏，FIX_REQUIRED/非终审不清；kimi.plugin.json 注册 PostToolUse 钩子；prompt-submit 提示点名派发 feedback-observer；修正词表收窄高信号集补「应该是/重做」（「能不能/为什么」类宽词不进表）。② REQ-081——feedback record 增 --scores/--evidence（内联 JSON；四维度 accuracy/coverage/efficiency/satisfaction 1-5 整数严格校验；带分数条目缺任一维度依据 exit 1 且不落盘，无分数条目拒收 --evidence；flag 循 CONTRACT_FLAG_EXTENSIONS 先例单列导出，不扰动 REQ-056 测试锁定表）；feedback-writer SKILL 模板与评分节补 scores_evidence 逐维必填；evolution-engine SKILL 补三节：双向扫描（每轮同扫"退"的候选：个例伪装规律/重复覆盖/已被可执行 check 取代，净规则量往下走，退役留指针）/ 强制抽象检验（换个产品还成立吗——不成立落项目层不进框架层，提议必附检验结论）/ 最小干预阶梯（例子 > 规则 > 改 Skill > 建新 Skill，每条提议声明层级并说清更低一级为何不够）。③ 文档同步：rules/large-repo.md hooks 接线表增 post-edit 行、stop/prompt-submit 行改写、运行态文件清单补 review-dirty.json；Product-Spec.md 摘 REQ-080/081 planned 标记（REQ-067 机制）+ 版本 v3.2.0→v3.2.1，CHANGELOG v3.2.1 同 commit 条目。宿主能力核实（官方 hooks/plugins 文档 2026-09-10 实读）：PostToolUse 事件存在且为观察型（matcher=工具名，返回值不影响主流程）；Stop/UserPromptSubmit 为可阻断事件；插件 manifest hooks 与全局 [[hooks]] 同事件面、同四字段——三事件全部宿主原生支持，零降级。④ 中途踩坑修复：版本行写「REQ-080/081 已激活」触发 spec lint 自指陷阱（行 3 被解析为需求声明报 NOT_NORMATIVE）→ 改写为不含 REQ id 的表述。证据（退出码均无管道直跑，2026-09-10 本机亲跑）：node --test tests/hooks-ext.test.mjs 10/10；node .kimi-base/audit/run-tests.mjs 16 文件全绿 exit 0；selftest 27/27 exit 0；npm run manifest 重生成（114 文件，digest fd4681842112）后 manifest --check exit 0；doctor ./pack-check/spec lint/trace/sync-check/rules-audit/arch check/catalog lint 全 exit 0；dod exit 0（PASS=13 FAIL=0，STALE=1 receipt-verify 为 strength policyHash 既有陈旧——并行会话改 strength 所致，STALE 按设计不阻断 dod，发布前重跑 gate 刷新）。一次性 flake 如实记录：首轮全量跑 tests/layers-adoption.test.mjs W1 并发 install 用例红一次，单跑绿、次轮全量绿，与本改动无因果（未触 installer），判并行会话干扰。

- v3.0 P12 REQ-078 ui-slop 可执行检查（2026-09-10；红测先行兑现：tests/audit.test.mjs 纯追加「ui-slop」8 例起步 8 红——红因核对为特性缺失本身（MODULE_NOT_FOUND ui-slop.mjs / copyfile ENOENT），实现未动任何既有断言；修复轮实现缺陷一：git ls-files -z 误 split(' ') 应为 split('\0')，红测当场抓到（scanned=0 假 SKIPPED））：① 新增独立审计脚本 .kimi-base/audit/ui-slop.mjs（零依赖、禁 import 引擎、stdout 单行 JSON + stderr 人类摘要；exit 0 干净允许 warning / 1 有 error / 3 降级：规则文件缺失损坏非法 reason=rules-file-unreadable|rules-file-invalid、非 git 仓 reason=not-a-git-repo；无前端文件 exit 0 + {skipped:true, reason:no-frontend-files} 响亮声明「放行 ≠ 全部通过」）+ 数据驱动规则文件 .kimi-base/audit/ui-slop-rules.json（8 规则：error=banned-font 禁字体 Inter/Roboto / purple-gradient-tells 紫蓝渐变 tell 色族 16 个 indigo·violet·purple 系 hex / tailwind-purple-ramp 紫蓝坡道渐变类 / marketing-copy 营销套话（Elevate your workflow/Supercharge/无缝/赋能类）；warning=hardcoded-hex 硬编码 hex 无 token·变量引用（exempt 行级 var(/--x:/$x:/tokens. + exemptFiles token·theme·palette 定义文件）/ backdrop-blur-unjustified 毛玻璃无理由 / slop-card-combo rounded-2xl+shadow-lg 全家桶 / emoji-as-icon emoji 当列表图标；exempt/exemptFiles/flags 全在数据文件，随设计演进只改 JSON 不改代码；ui-slop:ignore 同行/上一行抑制留痕同 scan-instructions 惯例；*.test.*/__tests__/dist/build 排除——测试夹具合法携带 tell 字符串）。② CI 接线三面：本仓 .github/workflows/ci.yml（dogfood，指令扫描后插 UI slop 步）+ templates/github-gate.yml（uislop 步 blocking 入汇总判定）+ templates/gitlab-gate.yml（step UI-slop检查 + 头注释管线描述同步）；docs/OPERATIONS.md §10 五脚本→六脚本 + 表行 + CI 链描述同步。③ dod 不接入 DOD_STEPS 的评估结论（否决）：dod 绿灯测试硬断言 PASS=9，接入必须改既有断言违反纯追加纪律；REQ-078 本体声明形态为「audit 脚本或 fitness 规则」且验收为「本仓 audit 电池接线」（CI 第三道闸），audit 脚本职责在独立执法面，dod 九步是引擎自检电池，层次不混。证据（无管道取码，2026-09-10 本机亲跑）：node --test tests/audit.test.mjs 39/39（新增 8 全绿）；真实夹具实证——slop 夹具（src/App.jsx 紫渐变+Inter+Elevate your workflow+backdrop-blur）exit 1，3 error（banned-font:4 / marketing-copy:4 / purple-gradient-tells:5）+3 warning（slop-card-combo:3 / backdrop-blur:3 / hardcoded-hex:5）逐条带 file:line；干净夹具（token 引用+ui-slop:ignore 语义注释）exit 0 零发现；本仓自跑 exit 0（51 前端文件 0 发现）；run-tests.mjs 16 文件 439/439 exit 0；selftest 27/27 exit 0；gate PASS=4 exit 0；catalog lint / check-syntax / scan-secrets / pack-check 全 exit 0；manifest --check 重基线后 exit 0。偏差如实记录：dod 在本会话窗口内 exit 2——FAIL 面为 manifest-check/pack-check（并行 REQ-077/079/080/081 实现者持续写载荷致基线反复漂移，漂移文件 large-repo.md/evolution-engine/feedback-writer 等均非本任务文件）与 unit（并行 REQ-081 红测 --scores/--evidence 未落地）/attributes（证据陈旧待 gate 刷新），均与本改动无因果；Product-Spec.md REQ-078 planned 标记按分工留主代理统一摘。

- v3.0 P10/P9 修复轮（2026-09-08，评审发现三处；红测由测试作者修订/增补并注明理由——eval.test.mjs 计数口径改 regression 单列（合计口径可被 capability 注水绕过：空套+20 capability 也全绿）+ spec.test.mjs P9 锚点组增 gitlab run-eval 锚点；实现未动任何测试断言）：① run-eval.mjs 计数门禁改 regression 单列——regression ≥20 且非空，capability 不计入（脚本头注释同步修订），漏洞形状「空套+20 cap」「19 reg+5 cap（合计 24）」均 exit 1 响亮报「capability 不计入门槛」。② gitlab-gate.yml 增补 run-eval regression 步（step 自我eval-regression，置于行为测试与 DoD 之间，与 github 变体同语义；头注释管线描述同步），采纳者删除指引注释同 github 版。③ github-gate.yml 汇总判定 report() 的 pipefail 脆弱性修复：correctness 评审实证 report() 在 advisory+skipped 形状下经 `[ "$3" = blocking ] && fail=1` 短路由返回 1，被 GitHub shell:bash（即 bash -eo pipefail）的 -e 提前中止、exit $fail 被绕过（复现：旧形 RISK=skipped → exit 1，本应为 0）；修复为 if/then + return 0——汇总打印恒 true，判定只走 exit $fail。验证（无管道取码，2026-09-08 本机亲跑）：模板内嵌 bash 块提取后 bash -n 通过、与实测脚本逐字节 diff 一致；五场景逻辑实证（全 success→0 / advisory+skipped→0 且 11 行完整 / 阻断 failure+advisory skipped→1 且 11 行完整点名 [FAIL] / 阻断 skipped→1 / advisory failure→0 [ADVS] 可见）；node --test tests/eval.test.mjs 19/19、tests/spec.test.mjs 63/63；npm run manifest 重生成（107 文件，digest 3272e4849c19）；run-tests.mjs 15 文件 421/421；run-eval 本仓 regression 20/20 exit 0；selftest/catalog lint/spec lint/trace/adr check/sync-check/manifest --check/doctor/pack-check 全 exit 0；gate PASS=4 刷新证据 → dod exit 0。
- v3.0 P10 REQ-066 自我 eval 套件（2026-09-08；红测先行兑现：tests/eval.test.mjs 由独立写测者先行（写测时点 15 红 1 绿 1 skip），本实现未动任何既有断言——唯一测试改动是按写测者注释约定摘掉 CI 引用用例的 { skip } 标记及其说明注释）：① 新增独立审计脚本 .kimi-base/audit/run-eval.mjs（零第三方依赖、禁 import 引擎，承 ADR-0002/REQ-037）——读 tests/eval/manifest.json（{version, regression:[], capability:[]}，内联任务对象或相对路径两种形态），judge.command 在临时目录跑子进程断言退出码（expectExit 缺省 0 / expectStdout 可选；{REPO} 占位符替换为正斜杠仓根 + KIMI_BASE_EVAL_REPO 环境注入，Windows cmd/sh 双壳可移植），judge.assertFile 断言文件存在且非空；regression 失败 exit 1 且 failures 点名任务 id；capability 默认只报 total、--include-capability 执行并报告 passed/failed 但永不阻断；任务总数 <20 或 tests/eval 缺失 exit 1（没跑够就是没证明）；诊断走 stderr、stdout 末行单行 JSON。② tests/eval/ 套件本体 23 任务（≥20 达标）：regression 20——引擎自检/doctor/catalog lint/arch check/adr check/spec lint/trace/skills-lint/agents-lint/rules-audit/pack-check/manifest --check/scan-secrets/scan-instructions/check-syntax（14 个本仓真跑）+ 未知 flag 拒 exit 1（REQ-056）+ gate 阻断 FAIL 检查 / 缺命令必 BLOCKED（REQ-013/NFR-005，最小夹具内联构造）+ planned 缺 phase 报 PLANNED_NO_PHASE（REQ-067 夹具）+ run-tests 无测试 exit 3 降级（NFR-005）；capability 3——runtime 全树零网络 import（NFR-004 终态，当前失败：supervisor 健康探针 2 处）/ install→种子落地→doctor 端到端（当前通过）/ regression 套增厚 ≥30（当前 20，爬坡中）；每任务六字段齐备、trace 锚真实 REQ/NFR id。③ CI 接入：templates/github-gate.yml 行为测试后增「自我 eval（regression 套）」阻断步并入汇总判定（注释明示采纳者无 tests/eval 须删步——套件是维护面不进安装面）；本仓 .github/workflows/ci.yml 同步接入 dogfood。④ 质量纪律实证：三个 regression 任务故障注入验证真红——暂存语法坏文件 → run-eval exit 1 点名 reg-check-syntax；暂存拼接构造的假凭据 → exit 1 点名 reg-scan-secrets；manifest 翻转 reg-unknown-flag 期望退出码 → exit 1 点名 reg-unknown-flag（三处现场均已还原，git status 零残留）。⑤ 落地过程缺陷一修：夹具脚本内 REQ-901 字面量被本仓 trace 判悬空引用（reg-trace 红）→ 改拼接构造（同 scan-secrets 金丝雀纪律）。摘 Product-Spec.md REQ-066 planned(P10) 标记 + CHANGELOG v3.1.7 段 + 版本号 v3.1.6→v3.1.7。证据（无管道取码，2026-09-08 本机亲跑）：node --test tests/eval.test.mjs 17/17（skip 0）；run-eval 本仓 regression 20/20 exit 0、--include-capability 报 passed:1/failed:2 仍 exit 0；run-tests.mjs 15 文件 418/418 exit 0；selftest/catalog lint/spec lint/trace（75/75 100%，planned 0）/adr check/sync-check/manifest --check（digest 7ef042993863，107 文件）全 exit 0；全批文件落盘后 gate PASS=4 exit 0（catalog-lint/arch-check/fitness/selftest）刷新指纹证据 → dod exit 0（STALE 响亮不阻断）。
- v3.0 P9 CI 发布面扩展（2026-09-08；轻量内容面，无 REQ 变化故 Product-Spec/CHANGELOG 不动；红测先行：tests/spec.test.mjs 追加「资产锚点：CI 门禁模板（P9）」2 例起步 2 红——红因核对为交付缺失本身：gitlab-gate.yml ENOENT / github-gate.yml 缺 schedule；实现未动任何既有断言。过程事故如实留痕：首次追加误用 Write 覆盖 tests/spec.test.mjs 致 1067 行丢失，git checkout 恢复后改 append 模式重投，零残留）：① 新增 .kimi-base/templates/gitlab-gate.yml——与 github-gate 同语义十段管线（selftest→check-syntax→scan-secrets→scan-instructions→manifest --check→run-tests→dod→arch trend --gate→risk 建议→doctor），GitLab 结构键齐备（stages/script/rules；MR/默认分支/schedule/web 四类来源）；GitLab script 任一行非零即中止，故改 step() 捕获退出码 + 末尾汇总收口；周检 cron GitLab 不在 YAML 表达，注释指引 CI/CD→Schedules（rules 已放行 schedule 来源）。② github-gate.yml 增强（对齐 cc-base 实证实践）：增 schedule 触发器（cron '17 3 * * 1' 周一 03:17 UTC 错开整点，环境腐烂早发现）；全部步骤 continue-on-error + 赋 id，末尾「汇总判定」步骤（if: always()，shell: bash）逐步骤报 PASS/SKIP/FAIL/ADVS——阻断步 skipped 也判失败，risk 维持 advisory，显式声明「放行 ≠ 全部通过」；顺带补 doctor 步与 GitLab 版对齐。③ release 接入强度 floor 可见性（ADR-0008 OPERATION_FLOOR.release=strict）：lib/release.mjs 就绪判定增 strength 一行（loadStrengthConfig 缺失=如实「未开启」；存在则 resolveStrength(ctx,{operation:'release'})，显示当前生效档+来源+floor 抬升轴数；解析异常经 attempt 降级可见），cli.mjs release case 输出首行附「强度 floor：…」；八条件本身未动。④ docs/OPERATIONS.md 第 10 节 CI 段补两模板实证实践说明、第 11 节 release 行补 floor 可见性。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：红态实锤 spec 60/62 红 2 → 转绿 node --test tests/spec.test.mjs 62/62、node --test tests/scale.test.mjs 24/24（release 五例零回归）；release floor 行双实证——本仓（无 strength.json）输出「强度治理未开启」、explore 档夹具输出「operation=release floor → strict 档下限，抬升 12 轴」；npm run manifest 重生成（106 文件，digest b987f3331da9，audit/manifest.mjs --check 双实现一致 exit 0）；selftest 27/27、spec lint、trace（74/74 100%）、sync-check、manifest --check 全 exit 0；dod 先 exit 2（改动引擎致指纹漂移、attributes UNCOVERED——既定语义非缺陷）→ gate PASS=4 刷新证据 → dod exit 0（PASS=13 STALE=1 响亮不阻断）。偏差如实记录：node .kimi-base/audit/run-tests.mjs exit 1，15 红全部位于 untracked 的 tests/eval.test.mjs（并行工作流的 REQ-066 评测套件红测先行，tests/eval/ 与 audit/run-eval.mjs 尚未实现，属另一实现者范围，与本改动无因果）；除该文件外 14 个测试文件逐一 node --test 全绿。

- v3.0 P8 评审修复轮三（2026-09-08；终审收尾 2 warning 转红测：tests/layers-adoption.test.mjs 纯追加「P8 修复轮三」2 例，起步 2 红——红因实证：uninstall 全程不落 marker（轮询 2s 窗口内 marker 从未出现）/ snapshots 清空后 gate 输出无交叉核对退化标注；实现未动任何既有断言）：① W5 uninstall 事务落 marker——installer.mjs 抽 acquireMaintenanceMarker/releaseMaintenanceMarker 公共原语（wx 原子独占创建 + installId 匹配才删，install/upgrade/uninstall 三面共用，消除轮二手写拷贝）；applyUninstallPlan 走同一原语全程持 marker，uninstall 中途 install/治理动词经既有 assertNoMaintenance/wx 面自然被拒 exit 3；新增测试用慢速钩子 KIMI_BASE_UNINSTALL_SLOW_MS（对齐 KIMI_BASE_INSTALL_FAIL_AFTER 先例）拉长事务窗口使并发观测确定性成立。② W6 arch.mjs gate 分支：snapshots 为空而 persisted bestEver 存在时，输出如实标注「无快照可交叉核对，persisted bestEver 为唯一判定依据」（机器行为不变）；本地棘轮残余面（整文件重写不可防，committed 趋势文件+git 史是缓解）写入 arch.mjs 注释与 docs/PROTOCOLS.md §5（与账本残余面同段声明）。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：红态 2 红实锤 → node --test tests/layers-adoption.test.mjs **29/29** exit 0；npm run manifest 重生成（105 文件，digest 1193aacb96ff）；node .kimi-base/audit/run-tests.mjs 14 文件 **398/398** exit 0；gate --risk high PASS=9 exit 0；dod exit 0（PASS=13 FAIL=0 STALE=1）；selftest、catalog lint、spec lint、trace、adr check、sync-check、manifest --check 全 exit 0。

- v3.0 P8 评审修复轮二（2026-09-08；终审 correctness 4 warning 转红测：tests/layers-adoption.test.mjs 纯追加「P8 修复轮二」4 例，起步 4 红——红因逐条实证：并发两 install 实得退出码 [0,0] 双放行 / bestEver=999 抬升后新债 gate exit 0 静默放行 / matrix 同名 fitness 必败命令被 id 去重误跳过 dod exit 0 / marker 存在期间 uninstall exit 0；实现未动任何既有断言）：① W1 installer.applyInstallPlan 的 marker 落盘从 atomicWrite 改 `open(markerPath,'wx')` 原子独占创建（EEXIST 即并发败者/残留 → degradedError exit 3 拒跑；先落 marker 再建 staging，败者零事务残留；前置 readMaintenanceMarker 预检保留为带 installId 详情的清晰报错快路径）——顺手修一处自伤：wx 前补 mkdir 父目录（首轮提交漏了，install 全灭 ENOENT，被自己测试当场拦住）。② W2 arch.mjs gate 分支交叉核对：persisted bestEver 与现存 snapshots 逐指标 min 取更严者判定；persisted 被抬高到比快照还松 → 报告以「bestEver 交叉核对：…不一致，篡改/腐化嫌疑」响亮报出（不静默放行）；REQ-064 截断场景（persisted 严于快照）行为不变。③ W3 hygiene.runDod 去重改 id+命令面双条件（checkInvocation display vs 电池 `node .kimi-base/runtime/kimi-base.mjs <args>`，或 builtin 映射同参）：同 id 同命令面才跳过；同 id 不同命令面必执行。与修复轮一既有用例（同 id 异命令 PASS 折叠为一行）的调和语义：异命令面副本照常执行，同结果 PASS 折叠显示（同 id 步骤行只出现一次，collapsed note 披露），异结果响亮单列——执行不重不漏，显示不重复确认。④ W4 applyUninstallPlan 过 marker 面：marker 存在即拒跑 exit 3 点名（维护中卸载会把半装态目标拆光）。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：红态 4 红实锤（W1 实得 [0,0]）→ node --test tests/layers-adoption.test.mjs **27/27** exit 0；npm run manifest 重生成（105 文件，digest 45089a6d9d15）；node .kimi-base/audit/run-tests.mjs 14 文件 **396/396** exit 0；gate --risk high PASS=9 exit 0；dod exit 0（PASS=13 FAIL=0 STALE=1；deduped note=catalog-lint/arch-check/adr-check，collapsed note=fitness——本仓矩阵 fitness 命令无 --all 与电池不同面，如实各自执行）；selftest、catalog lint、spec lint、trace、adr check、sync-check、manifest --check 全 exit 0。

- v3.0 P8 评审修复轮（2026-09-08；红蓝评审 verdict=FIX_REQUIRED：C1 marker 无独占 / C4 protected 可标 outer / C5 dod 忽略 check.cwd / C2+C3 bestEver 无形状防护 / dod 头条裸「通过」/ dod 与电池重复执行；同授权形态测试与修复同人，严格先红后绿：tests/layers-adoption.test.mjs 纯追加「P8 修复轮」8 例（起步 6 红 2 绿——C1 install 完成移除 marker 与主流程本绿），红因逐条实证：外来 marker 下 install exit 0 且 finally 误删他人 marker / sec-outer 配置面放行 / dod 对 cwd=src 检查 FAIL（gate 同检查 PASS）/ bestEver="abc" gate 静默放行 / outer FAIL 头条裸「dod 通过」/ catalog-lint 在 dod 输出出现两次；实现未动任何既有断言）：① C1 installer.applyInstallPlan 事务前先 readMaintenanceMarker——已存在即 degradedError exit 3（点名 marker/installId/since + 恢复指引）；finally 移除改为 installId 匹配才删（他人 marker 绝不误删）。② C4 matrix.mjs validateMatrix 增 protected≠outer：kind 或认领属性命中 security/safety/privacy 且 tier=outer → MATRIX_INVALID exit 1 点名 id——保护底线永不信号性降级。③ C5 hygiene.runMatrixCheckStep 传 check.cwd（path.resolve(ctx.root, cwd)，isPathInside 逃逸即 FAIL，与 gate 同语义）。④ C2/C3 arch.mjs 增 assertBestEverShape（非对象/非数字/负数 → HarnessError exit 1 并给修复路径，棘轮不静默禁用），record/gate 两入口都校验；record 交叉核对三方 min（持久化值/现存快照/当前测量），持久化值被抬高时由快照拉回真实下限。⑤ cli.mjs dod 头条：outer 有 FAIL 时为「dod 通过（inner/middle 全绿；outer 有 N 项 FAIL 响亮可见——趋势健康信号不阻断）」，无 outer FAIL 保持裸「dod 通过」。⑥ 去重：matrix 检查 id 命中 DOD_STEPS 电池 id 即跳过（电池步骤同语义同入口已跑），deduped 清单以 note 行披露。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：红态 23 例 6 红实锤 → node --test tests/layers-adoption.test.mjs **23/23** exit 0；npm run manifest 重生成（105 文件，digest b9cd7411e963）；node .kimi-base/audit/run-tests.mjs 14 文件 **392/392** exit 0；selftest、catalog lint、spec lint、trace、adr check、sync-check 全 exit 0；gate --risk high PASS=9 exit 0；dod exit 0（PASS=13 FAIL=0 STALE=1，去重 note 行实证 catalog-lint/arch-check/adr-check/fitness 只跑一遍）。教训记录：本轮曾先红后绿节奏中出现一次自选动作失误——manifest 重生成后、gate 运行期间又改 cli.mjs 载荷，致 engineHash/manifest 漂移连锁 FAIL（pack-check/manifest-check/unit 三红）；按 layout 铁律「改载荷同 commit 重算 manifest」重新生成并复跑后全绿，确认为操作顺序问题而非实现缺陷。

- v3.0 P8 分层反馈与渐进采用（2026-09-08；REQ-062/063/064/065；红测先行：tests/layers-adoption.test.mjs 15 例起步 13 红 2 绿，红因核对为行为缺失——matrix 无 tier schema / discover 无 --level / trend 无 bestEver 持久化 / 无 maintenance marker；实现未改任何测试文件与断言）：① REQ-062 三层反馈分级——matrix.mjs schema 增 tier（合法集 inner/middle/outer，非法值经 validateMatrix 任何入口 exit 1 点名 id+合法集；缺失的必填执法在 assertCheckTiers，由 gate --dry-run 配置校验面调用），dod 把 matrix 检查按层纳入：DOD_STEPS 单源扩 tier 字段（静态电池 inner/middle），matrix 检查经 gate.mjs 导出的 checkInvocation 单源推导调用形态后作真实子进程执行（不写回执/账本——dod 是体检电池不是取证门），cli.mjs dod 输出按 inner/middle/outer 分组，outer FAIL 响亮可见（同行 id+FAIL+汇总行）但不阻断退出码，inner/middle FAIL 维持 exit 2。② REQ-063 渐进采用阶梯——discover.mjs 增 level 选项（DISCOVER_LEVELS=L0..L3，缺省 L1=现状逐字节一致，非法值 usageError exit 1 列合法集）：L0 裁掉 draft.layers/module.layer/module.attributes/attributeProposals 及对应 needsDecision；L2 保留 layers+attributeProposals；L3 输出带 fleet 仓群治理引用。--level flag 经 cli-contracts.mjs 新增 CONTRACT_FLAG_EXTENSIONS 单列导出接线（见 Decisions）。③ REQ-064 bestEver 持久化——arch.mjs archTrend --record 把逐指标历史最优显式写入 arch-trend.json 的 bestEver 独立字段（min(持久化值, 当前值)，永降）；--gate 只信持久化 bestEver（旧格式状态文件无 bestEver 字段时退化回快照现算 min，REQ-016 行为不变）。④ REQ-065 maintenance marker——paths.mjs 增 MAINTENANCE_MARKER_REL；installer.mjs applyInstallPlan 非 dry-run 时在 staging 建立后写 .kimi-base/state/maintenance.json，finally 移除（提交/回滚都移除，仅进程中断遗留）；state.mjs 增 readMaintenanceMarker/assertNoMaintenance（degradedError exit 3 点名 marker）；cli.mjs needProject 统一接入（全部治理动词）+ admin.mjs doctorCommand 已安装模式接入；dry-run 零写入与 FAIL_AFTER 故障注入逆序回滚回归保持。本仓 dogfood：verification-matrix.json 与 example 种子全部检查补 tier 标注。Product-Spec.md 摘 REQ-062~065 planned(P8) 标记 + Product-Spec-CHANGELOG v3.1.6 段 + 版本号 v3.1.3→v3.1.6（头部版本此前两轮未跟进，本轮一并追上 CHANGELOG 链）。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/layers-adoption.test.mjs **15/15** exit 0；npm run manifest 重生成（105 文件，digest 8d528223e7a3）；node .kimi-base/audit/run-tests.mjs 14 文件 **384/384** exit 0；selftest、catalog lint、spec lint、trace、dod、adr check、sync-check 全 exit 0。

- v3.0 P7 quarantineState 并发竞抢修复轮（2026-09-08；评审发现：3 进程并发触发同一损坏文件隔离时，输家 rename ENOENT 崩溃 exit 3 且高危发现丢失。先红后绿同授权形态：tests/robustness.test.mjs REQ-061 段纯追加 1 例——5 并发 `task status` 打同一损坏 tasks.json，损坏点放在 2MB 文件尾部拉大 read→rename 竞抢窗口保证重叠，断言全进程无 exit 3/无 ENGINE_ERROR、.corrupt-* 产物恰好一个、quarantine.jsonl 有记账；红因当场复现=输家 `内部错误[ENGINE_ERROR] ENOENT rename` exit 3，既有 9 例未动）：修法按评审定案落 state.mjs quarantineState——rename 包 try/catch，ENOENT 时用 pathExists 复核源文件：源已消失=赢家已隔离，容忍直返（记账归赢家，不重复刷屏）；源仍在却 ENOENT 属异常不吞、ENOENT 以外错误照抛。修复后：node --test tests/robustness.test.mjs **10/10** exit 0（连跑 4 次全绿排 flake）；npm run manifest 重生成（105 文件，digest 0fad4d1675ff）；node .kimi-base/audit/run-tests.mjs 13 文件 **369/369** exit 0；selftest、dod 全 exit 0（dod 的 STALE=receipt-verify 指纹随工作树移动，按既定语义不阻断）。
- v3.0 P7 REQ-059/060/061 健壮性三件套（2026-09-08；红测先行：tests/robustness.test.mjs 9 例起步 5 红 4 绿，红因核对为行为缺失——gate/quality/dod 未过项无 nextStep / ledger-head 损坏只内存打标 / agents-lint 阈值未锁 6000；实现未改任何测试文件与断言）：① REQ-060 修复指令体——单源 helper `lib/core.mjs nextStepFor(kind, context)`（gate-fail/gate-blocked/quality-uncovered/dod-step 四类，拒绝"请检查"式空泛，全部指向重跑命令或 .kimi-base/verification-matrix.json 配置路径）；cli.mjs gate FAIL/BLOCKED 回执条目与 quality status UNCOVERED 条目渲染时拼接 nextStep（不入账本，回执形状不变）；hygiene.mjs runDod 给 FAIL 步骤（含子进程无法启动归级的 FAIL）附 nextStep，cli.mjs dod 输出行渲染。② REQ-061 quarantine 收尾——state.mjs quarantineState 导出为唯一隔离原语；ledger.mjs readLedgerHead 损坏分支从内存 {__corrupt:true} 静默兜底改为真实隔离（.corrupt-<ts> rename + quarantine.jsonl 记账），"锚被篡改"高危告警语义并存（reconcileLedgerHead 仍按伪造面对账，risk scan exit 2 + state-quarantined 双命中）；直读点补齐：latestReceipts 损坏回执隔离后按"无 fresh 证据"继续（不拖死读者）、strength.mjs readStrengthState 损坏走隔离但当场重抛 fail-closed（强度态决定门禁档位，不静默按默认档继续）；tasks.json/fast-mode.json 本就走 readState 原语（测试复核确认）。③ REQ-059 宪法预算——agents-lint 收敛为 >6000 字节 error exit 1（取消 12000 warning/16000 error 中间档），cli.mjs help×2+打印行、docs/PROTOCOLS.md §14.3、.kimi-base/rules/large-repo.md 阈值文案同步；本仓 AGENTS.md 4009 字节达标未动。Product-Spec.md 摘 REQ-059/060/061 planned(P7) 标记 + Product-Spec-CHANGELOG v3.1.4 段同 commit。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/robustness.test.mjs **9/9** exit 0；npm run manifest 重生成（105 文件，digest 8c958339b6ca）；node .kimi-base/audit/run-tests.mjs 13 文件 **368/368** exit 0；selftest、catalog lint、spec lint、trace、dod、adr check、sync-check 全 exit 0。
- v3.0 P5 REQ-057 评审修复轮（2026-09-08；评审 4 warning 转红测：独立测试作者 tests/review.test.mjs 末尾纯追加「P5 评审 warning 回归」W1-W4 四例，红因逐条核对为缺陷本身；实现未动任何测试文件与断言）：① W1 身份匹配归一化——reviewVerdict 作者/执行者匹配改 trim+toLowerCase 归一后比较（"Main-Agent" 与 "main-agent" 判同一人），改名大小写不再绕过作者自审拒判。② W2 诚实标注进回执——ACCEPT 回执对象携带 authorshipEnforced 布尔字段（消费者只认回执，标注不能只在 CLI 输出）。③ W3 ad-hoc 不进独立性判定——执行者集只统计非 adHoc 的 lens 报到记录（ad-hoc 是补充证据通道：发现计入裁决，执行者不参与作者自审判定），作者追加 ad-hoc 不再误触拒判。④ W4 pack 静态发现对准评审 range——collectStaticFindings 接收会话：range 模式时 fitness 改扫 session.scope.paths（range 变更文件集）、budget 改以 range.base 为基线；range 评审改动已提交、工作树干净时不再「扫描 0 文件」静默漏报；arch check 是声明图全仓校验（不消费 changedPaths）无变更面可对准，保持现状；pack 节标题如实标注扫描面口径。顺手 info 级修复：range 作者集 git log 采集失败时 authorshipEnforced 如实降 false（authorsComplete 闸门），不静默按已执法报。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/review.test.mjs **42/42**（W1-W4 转绿，既有 38 零回归）；npm run manifest 重生成（105 文件，digest acf6301bd85b）；node .kimi-base/audit/run-tests.mjs 12 文件 **359/359** exit 0；selftest、catalog lint、spec lint、trace、adr check、sync-check、**dod 全 exit 0**——上轮偏差（fitness 命中 tests/review.test.mjs:865/868 纯字面量金丝雀）由测试作者按仓内拼接构造惯例（同 harness.test.mjs LESIONS 先例）修复，本仓 fitness --all 复跑 exit 0 实锤，dod 转绿与本实现无因果外的关联。
- v3.0 P5 REQ-057 评审强化（2026-09-08；ADR-0008 第 8 条；红测先行：独立测试作者 tests/review.test.mjs REQ-057 段 10 用例 + tests/cli-contracts.test.mjs EXPECTED_FLAGS.task 扩 author 两条，红因核对为行为缺失——task 无 author 字段 / --author 未注册 / recordLens 忽略 --reviewer / verdict 无作者集概念 / pack 无静态发现节 / reviewStart 不消费 strength.json；实现未改任何测试文件与断言）：① authorship 账本接线——cli-contracts task 契约 flags 增 author:value；tasks.mjs taskStart 写 author（String(input.author ?? '').trim() || 'main-agent'）；cli.mjs taskStart 透传 flags.author、recordLens 透传 flags.reviewer；recordLens 把非空 reviewer 写进 session.lenses[name].reviewer。② reviewVerdict authorship 执法——作者集 = active task.author ∪（range 模式）git log --format=%an 提交者集；执行者集 = 各 lens 报到 reviewer；两侧都有数据且交集非空 → SELF_REVIEW_REJECTED exit 2（输出点名「作者自审」+ authorshipEnforced:true，绝不写回执）；齐备无交集 → 照常裁决 + authorshipEnforced:true；任一侧无数据 → 照常裁决 + authorshipEnforced:false（不假绿）；verdict CLI 输出恒带 authorshipEnforced 行。③ reviewStart 消费强度策略轴——loadStrengthConfig(ctx) 非 null 时 resolveStrength(ctx, {}) 驱动：reviewLenses full→LENS_LIBRARY 九 lens 全集 / minimal·none→['correctness']（地板）/ standard→team 相当集，再套既有属性收缩（selectReviewTeam 抽出 shrinkByAttributes 复用，无 strength.json 路径逐字节保持现状=回归锁）；maxRounds 取 axes.reviewRounds 定格进 session（verdict 优先 session.maxRounds，回落 catalog.review.maxRounds ?? 3）；strength 与 catalog 显式 lenses 冲突时 strength 胜出并在 session.strength.note 注明。④ reviewPack 新增「静态现存发现」节——fitness/arch check/budget 三源各包 try/catch（采集失败如实标注不炸包），fitness 报告原文（规则 id+路径+行号）入包；夹具埋点 no-secret-literal + src/leak.js 实测命中。复杂度纪律：review.mjs 增量 138 行（≤150）。REQ-057 摘 planned(P5) 标记 + Product-Spec-CHANGELOG v3.1.3 同 commit（同段记录 P11 批次二 REQ-072/073/074 摘标记——检验 docs/evals/p11-ab-test.md 已在树，兑现 v3.1.2「检验完成前不摘」条件）。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/review.test.mjs 38/38；node --test tests/cli-contracts.test.mjs 17/17；npm run manifest 重生成（105 文件，digest 7467f13944c4）；node .kimi-base/audit/run-tests.mjs 12 文件 355/355 exit 0；selftest、catalog lint、spec lint、trace、adr check、sync-check 全 exit 0。偏差如实记录：dod exit 2——唯一 FAIL 是 fitness 步命中 tests/review.test.mjs:865/868 的红测夹具凭据字面量（no-secret-literal）；该字面量是测试作者未按仓内「拼接构造」金丝雀惯例（对照 tests/harness.test.mjs:677 LESIONS 的 'AKIA' + 'IOSFODNN7EXAMPLE' 写法）留下的，修复权属测试作者（把夹具字符串拆段拼接即可），实现者按「严禁修改任何测试文件」纪律不动；引擎侧无法区分——夹具内容必须是规则必中样本，任何放宽都会同时放生 fixture。dod 的 STALE（receipt-verify exit 4，引擎树/指纹漂移）按既定语义不阻断。
- 教训（工具护栏）：前任实现者被 Read 整文件护栏中断——大文件（cli.mjs 1136 行）Read 必须带 line_offset/n_lines 分段读；整文件直读会被 >50000 字符护栏转储到 tool-results 文件，连续重试不解决问题，分段或 Grep 定位才是正路。
- v3.0 P6 REQ-058 评审修复轮（2026-09-08；红蓝评审 verdict=FIX_REQUIRED：correctness 1 error + 2 warning + info 级三项；本回合经主代理指示测试与修复同人，严格先红后绿：tests/feedback.test.mjs 追加「P6 修复轮」5 红测 + tests/spec.test.mjs 追加 1 红测，红因逐条核对为缺陷本身——10 并发 record 实得 occurrences=6（与评审实证一致的丢计数）/ 损坏条目拖死 list exit 1 / record 假失败 exit 1 但条目已落盘 / 负 occurrences -5 递增得 -4 / flaky--test 分裂成第二文件 / 围栏内假示例节放行缺节 skill；实现未动任何既有断言）：① record/skip 读-改-写全程入跨进程文件锁（state.mjs withFileLock，锁文件 .kimi-base/state/feedback.lock），10 并发 occurrences===10 且 INDEX 同步；② 损坏条目（缺 frontmatter）按 quarantine 精神隔离——quarantineEntry 重命名 <条目>.md.corrupt-<ts>（并发竞抢 ENOENT 静默放过）+ 四动词输出 warning 点名，readEntries 返回 {entries,warnings} 贯穿 list/scan/propose/record；record 重排为先校验输入、锁内「读/隔离→写条目→重写 INDEX」一气推进（命令失败即未计数，可安全重试，不再假失败真落盘）；record/skip 目标条目自身损坏时隔离后按新建/缺失语义处理；③ skills-lint 工艺检查标题收集加 FENCE 围栏状态机（与 lintCognitionLabels 同形态：闭 fence 同字符且长度≥开），围栏内模板片段的章节标题不再放行缺节 skill；④ info 级三修：occurrences 非法值（负数/非数）按 0 起计 +1（负计数永不毕业死条终结，读侧 toEntry 本就有 ≥1 钳制）、normalizeTopic 增连续连字符折叠、help 文案「只读/exit 恒 0」改如实描述（不改规则与有效条目 + 损坏隔离警告）。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：红态实锤 feedback 20/25 红 5 + spec 56/57 红 1（红因如上）→ 修复后 node --test tests/feedback.test.mjs 25/25、node --test tests/spec.test.mjs 57/57、node --test tests/cli-contracts.test.mjs 17/17；npm run manifest 重生成（105 文件，digest c08f7d28daeb）；node .kimi-base/audit/run-tests.mjs 12 文件 341/341 exit 0；selftest、catalog lint、spec lint、trace、skills-lint、arch check --scan、sync-check、dod 全 exit 0。
- v3.0 P6 REQ-058 feedback 引擎化（2026-09-08；ADR-0010 docs/adr/0010-feedback-engine.md；红测先行：独立测试作者 tests/feedback.test.mjs 20 用例全红起步 + tests/cli-contracts.test.mjs 扩表 39+help→40+help 三条红，红因核对为行为缺失（未知动词 feedback / 复制面无 .kimi-base/feedback）；实现未改任何测试文件与断言）：① 新增 lib/feedback.mjs（274 行，零依赖）——record（topic 归一化 trim+小写+空白/下划线折叠为连字符，同主题去重 occurrences+1 并刷新 updated，frontmatter 七键固定键序+额外键保留，description 单行化；FEEDBACK-INDEX.md 每次写入后从条目全集现算重写=引擎单一维护）/ list（五字段，templates/ 示例可见并标注，空目录 exit 0 显式报空）/ scan（只读：毕业候选 occurrences≥3 且未 graduated/skipped、同 type 跨 ≥3 topic 聚类候选、repeated-operation 且 occurrences≥5 → 新 skill 候选）/ propose（目标层映射 quality-issue→check、user-correction→fitness、uncovered-scenario/repeated-operation/skill-effectiveness→skill，未映射兜底 AGENTS.md 散文；每条带 feedback id+occurrences 证据指针；永不自动改规则——唯一写操作是 --skip 记 frontmatter skipped:true 并重写 INDEX）。② 接线：CONTRACTS 增 feedback 条目（topic/type/description/skip 均 value，positional 1..1）；cli.mjs 增 case + HELP_GLOBAL/HELP_VERBS 条目；scan.mjs ENGINE_VERBS 加 feedback；selftest contractCheck 双向钉死自动接管（路由/契约/help 40=40=40）。③ 载荷：.kimi-base/feedback/（FEEDBACK-INDEX.md + templates/ 示例条目七键齐备）经 installer 新增 CONDITIONAL_MANAGED_ENTRIES 条件纳入复制面（老载荷/夹具源仓无该目录不报错；面内仍走 isStableAsset 白名单=仅 INDEX+templates/，私人条目永不发布）；audit/manifest.mjs 独立实现 MANAGED_DIRS 同步对齐；package.json files 增 .kimi-base/feedback/；make-release.sh 私密条目剔除从「整个 feedback/ 剔除」改为按 isStableAsset 同口径只剔白名单外 tracked 条目（机制保留，ADR-0010 第 3 条），解包复核禁入面同步收紧。④ 同步面：module-catalog payload-assets 增 .kimi-base/feedback/**；AGENTS.md 速查/载荷枚举/目录地图、docs/ARCHITECTURE（lib 35 模块、41 动词、模块地图行）、docs/OPERATIONS 动词表、DEV-PLAN P6 ✅；REQ-058 摘 planned(P6) 标记 + Product-Spec-CHANGELOG v3.1.1 同 commit。偏差记录：installer 采用「条件受管面」而非 MANAGED_ENTRIES 硬条目——harness.test.mjs 的 sourceCopy 夹具镜像 MANAGED_ENTRIES 字面集且无 feedback 目录，硬要求会让既有 install 测试全部 SOURCE_SURFACE_MISSING（测试不可改）；条件纳入对真实源仓等价（目录在场即进复制面与 manifest）。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/feedback.test.mjs 20/20；node --test tests/cli-contracts.test.mjs 17/17；npm run manifest 重写（105 文件，digest b6ebe8667bf1）；node .kimi-base/audit/run-tests.mjs 12 文件 327/327 exit 0；selftest、catalog lint、arch check --scan、adr check、spec lint、trace、rules-audit、dod、sync-check 全 exit 0。
- v3.0 P11 REQ-069 认知标注四态进 spec lint（2026-09-08；ADR-0012，设计 docs/COMMUNICATION-LAYER.md §3；红测先行：tests/spec.test.mjs 追加「认知标注四态」5 用例，起步 3 红 2 空绿——红因逐条核对为行为缺失：推断无依据/未知无确认途径时 spec lint 报 exit 0 且无 SPEC_LABEL_* 输出、SPEC_UNLABELED warning 不存在；实现后未改任何既有断言，唯一测试侧改动是合法四态夹具的 REQ 条目从节内移到「## 需求清单」标题下——夹具布局缺陷非断言迁就）：lib/scan.mjs 新增 lintCognitionLabels 并在 specLint 文件循环接线——「现状与假设」二/三级标题开节、同级或更高级标题收节，节内列表条款（- 或数字开头）逐条判定：无标签 → warning SPEC_UNLABELED（未标按推断论处，存量迁移坡道）；[推断] 无「依据：/依据：」→ error SPEC_LABEL_NO_BASIS；[未知] 条款内无「确认」二字（宽口径，含「确认：」与责任方描述）→ error SPEC_LABEL_NO_VERIFY_PATH；fence/代码块豁免。exit 契约不变（error→1，warning 不拦）；本仓 Product-Spec.md 无该节零影响（spec lint 0 error 0 warning 复跑实证）。REQ-069 摘 planned(P11) 标记，Product-Spec-CHANGELOG v3.1.0 段同 commit 追记。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/spec.test.mjs 32/32；node .kimi-base/audit/run-tests.mjs 11 文件 exit 0；selftest、spec lint、trace、dod、manifest --check 全 exit 0。环境说明：收尾期间主代理并行工作流持续写 .kimi-code/ 载荷文件致 manifest 多次漂移（首次 run-tests 红因=并发写入非本改动），每轮验证前按布局铁律重算 manifest 后复跑转绿。
- v3.0 P11 REQ-069 确认途径口径放宽（2026-09-08；终审最后一个 warning：责任方+场合齐备的「由财务负责人在评审会答复」因缺字面"确认"被误报 NO_VERIFY_PATH；同一授权形态先红后绿：tests/spec.test.mjs 追加 2 例——正例先红（误报实锤）后绿、「确认率待提升」回归锁红前即绿；实现未动任何既有断言）：VERIFY_PATH 责任方形态放宽为 （由|待|请|找）…（确认|答复|回复|拍板|定论|给出）（窗口 [^，。；]{0,20} 不变），报错消息同步新口径；URL 尾字符回退泄漏（query 藏 CJK 依据时 NO_BASIS 漏报）属剥离策略固有方向，不修，COGNITION_URL 上方注释写明残余面（宁可漏报不误报）。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/spec.test.mjs 44/44；run-tests.mjs 11 文件 exit 0；selftest、spec lint、dod、manifest --check 全 exit 0。
- v3.0 P11 REQ-069 终审修复回合（2026-09-08；终审 FIX_REQUIRED：2 error + 2 warning，均为上轮修复引入/遗留；同一授权形态测试与修复同人，严格先红后绿：tests/spec.test.mjs 追加「认知标注四态·终审修复」5 红测，红因逐条核对为缺陷本身——节号标题不开节 exit 0 漏检 / URL \S+ 吞全角逗号后真依据误报 NO_BASIS / 「确认率」裸子串豁免 exit 0 / ~~~ 提前关 ``` fence 误报 a.md:8 / ``` 提前关 ~~~~ fence 误报 a.md:7；实现未动任何既有断言）：① F1 节标题与自家模板不兼容（error）——templates/Product-Spec.md 的「## 3. 现状与假设（认知标注）」带节号前缀不开节；COGNITION_HEADING 增可选节号前缀 (?:\d+[.、．]\s*)?，口径注释更新。② F2 URL 剥离吞全角标点（error）——\S+ 把「，依据：SRE 访谈」整段吞掉；COGNITION_URL 改终止于空白与中文句读（，。；、！？）且尾字符须为 URL 合法收尾（[A-Za-z0-9/#_~-]，尾部 ASCII 句读回收进正文）；URL 内部 CJK 与全角冒号照常剥离（既有 T2「依据在 URL 内不误算」回归保持绿）。③ F3 确认途径结构化——裸 includes('确认') 改为 /（由|待|请|找）[^，。；]{0,20}确认|确认[：:（(]|确认途径/，「确认率」不再豁免。④ F4 fence 开闭配对——记录开 fence 的字符与长度，闭 fence 必须同字符且长度 ≥ 开。⑤ 模板侧核对：templates/Product-Spec.md:34 标题在新口径下实测开节（node 直跑正则验证），模板不进 spec lint 扫描面，无需改。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/spec.test.mjs 42/42（新增 5 转绿、既有 37 零回归）；node .kimi-base/audit/run-tests.mjs 11 文件 exit 0；selftest、spec lint（error 0、SPEC_* 计数 0）、trace、dod、rules-audit、manifest --check（digest 01518c59717f，101 文件）全 exit 0。
- v3.0 P11 REQ-069 评审修复回合（2026-09-08；红蓝评审 FIX_REQUIRED：correctness 1 error + 4 warning；同 P4 授权形态测试与修复同人，严格先红后绿：tests/spec.test.mjs 追加「认知标注四态·评审修复」5 红测——红因逐条核对为缺陷本身：标签自包含/URL 依据绕过 exit 0 无 error、SPEC_LABEL_MULTI 不存在、~~~ fence 内行与提及性标题下条款被误报 SPEC_UNLABELED；实现未动任何既有断言）：① H1 标签 token 自包含绕过（correctness error）——[未知] 条款的确认途径判定是裸 includes('确认')，同行带 [确认] 标签即豁免；[推断] 的依据可被 URL 携带的"依据："命中。修复=形态检查改在剥离全部标签 token 与 http(s) URL 后的正文上进行；依据维持"依据：/依据："带冒号形态。② H2 多标签未执法——规格承诺"恰好一个"而实现只查 ≥1；新增 error SPEC_LABEL_MULTI（同条款 ≥2 个标签，逐条点名标签集；多标签不豁免后续形态检查）。③ H3 ~~~ 波浪 fence 不豁免——fence 判定从 ``` 扩为 ```|~~~（rules-audit 的同款旧判保留未动，属另一动词面）。④ H4 节标题子串过匹配——开节改精确口径：标题文字以「现状与假设」开头且其后为边界字符（空白/括号/冒号/破折号/行尾），口径写入代码注释。⑤ H5 agents 内容漂移——code-reviewer 派单包 6 bullet（Scope/Out of Scope 合并）拆为七字段；progress-recorder 8 bullet（多出 delta 字段）并入 Goal 表述、对齐 dispatch-contract.md 单源七字段。⑥ info 级记录不修：行文提及标签本身（「详见 [确认] 标签的用法说明」）会被误判已标——标签 token 辨识度高、误报面可接受，边界已写进 scan.mjs 注释。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/spec.test.mjs 37/37（新增 5 转绿、既有 32 零回归）；node .kimi-base/audit/run-tests.mjs 11 文件 exit 0；selftest、spec lint（error 0、SPEC_* 计数 0）、trace、dod、skills-lint、agents-lint、manifest --check（digest 88588f6b0af7，101 文件）全 exit 0。
- v3.0 P4 Receipt v2 绑定面 + fast 证据贷款账本 + 可提交证据模式（2026-09-07；REQ-053/054/055，ADR-0009；红测先行：独立测试作者 tests/receipt-v2.test.mjs 15 用例 12 红 3 绿起步，红因逐条核对为行为缺失（缺绑定字段/缺 DEFERRED 条目/缺 FAST_MODE_DEBT/harness 拒 evidence 段），实现未动任何测试文件与断言）：① 新增 lib/bindings.mjs——bindingSurfaces 三面：policyHash（strength resolveStrength 输出，无 strength.json 显式 null）/engineHash（正在执行的引擎 runtime 树逐文件 LF 归一化 sha256，组合口径同 manifest digest「path\0hash\0bytes」行拼接，state 运行态天然不在树内；绑引擎副本路径而非项目根）/catalogHash（module-catalog.json 内容 LF 归一化哈希，无则 null）；gate 全批回执（含 missingKinds 合成 BLOCKED 回执）增绑三面，键恒存在、无配置值 null。② verify.mjs——按「键存在」分版本：v2 回执任一面漂移 → stale exit 4 并点名 policyHash/engineHash/catalogHash（链完好不算篡改）；v1 旧回执（缺新字段）按 v1 绑定面（fingerprint）判定，不谎报篡改；DRIFT 语义收窄为「镜像自洽但回滚到账本旧条目」（镜像哈希能在该 check 账本历史中找到且非账本尾）——账本外自洽镜像（如剥掉 v2 字段重修哈希的 v1 回执）由新鲜度与绑定面判定，这是 v1 兼容用例与 gaps DRIFT 用例交集下的唯一语义；committed 模式下只动 .kimi-base/state/** 的证据入库提交不算指纹漂移（git diff baseCommit..HEAD 排 state 为空且工作树无非 state 变更；无法测量保守维持 stale），clone 换机 receipt verify 直接 exit 0。③ fast 贷款账本（REQ-054）——fast 窗口内每条被跳检查在回执之外追加 kind=deferred 账本条目（checkId/windowId/fingerprint/contentHash/chain 入哈希链；protected 与已执行 FAIL 走不到 SKIPPED 分支永不产 DEFERRED）；fast.mjs fastDebtOf：账本是债务唯一事实源，deferred 借、其后同检查无 fastWindow 印记的 verification PASS 还；hygiene risk scan 报 FAST_MODE_DEBT（high）直至偿清；关窗/窗口过期/删 fast-mode.json 均不清债；P7b 语义（fast 印记不能关闭 task/release）原样锁定。④ 可提交证据模式（REQ-055）——config.mjs 接受 evidence.mode: local|committed（严格校验，非法值配置期 exit 1 并点名非法值）；ledger.mjs stateGitignoreContent/ensureStateGitignore：committed 放行 ledger*.jsonl 与 receipts/，证据日志 evidence/*.log 与其余运行态永不入库（gitignore 机械保证）；installer 的 state/.gitignore 落地按目标 harness.json 模式区分（local 默认零变化）；gate committed 模式收尾 git add -- .kimi-base/state（ignore 规则决定入 index 面，证据日志物理上进不了 index；不自动提交——提交史是用户的）。cli.mjs help（gate/receipt/fast/risk 四条）同步新语义；cli-contracts 无新增 flag/verb，contractCheck 双向钉死不变。证据（均无管道直跑退出码，2026-09-07 本机亲跑）：node --test tests/receipt-v2.test.mjs 15/15；node .kimi-base/audit/run-tests.mjs 11 文件 274/274 exit 0；selftest、catalog lint、arch check --scan、adr check、spec lint、rules-audit、sync-check、dod、manifest --check 全 exit 0；trace exit 0（REQ-053/054/055 的 PLANNED_HAS_TESTS 为预期工作流——planned 标记摘除属规格作者面，本回合不动规格）；gate 全绿回执绑定当前指纹。
- v3.0 P4 评审修复回合（2026-09-08；红蓝评审 verdict=FIX_REQUIRED：correctness 4 error + testing 1 error，独立测试作者在 tests/receipt-v2.test.mjs 追加 5 红测——红因逐条核对为缺陷本身；实现修复未动任何测试文件与断言）：① D1 镜像伪造 fail-open（correctness）——上轮 DRIFT 收窄留下「账本外自洽镜像无罪」缺口（FAIL→PASS+重算 contentHash 即放行）；verify.mjs 修字段级对账：镜像哈希非账本尾且不在账本历史时，与账本尾条目按交集键逐字段 stableJson 对账（跳过镜像缺失的键——v1 回执缺 v2 三键不谎报篡改，既有 v1 兼容用例保持绿），任一不一致 → TAMPERED exit 2 点名检查。② D2 轮转清债（correctness）——fastDebtOf 只扫当前段，retention 轮转把未偿还 deferred 归档即成免债路径；ledger.mjs 新增 readLedgerHistory（归档段按文件名时间戳字典序=时序再拼当前段；链校验仍走 readLedgerEntries+anchor 续链不动），risk scan 的 FAST_MODE_DEBT 改吃全史——选定「扫归档」而非「轮转结转」：账本是追加即事实的系统，结转会在新段写入衍生条目，模糊事实与视图的边界。③ D3 committed 落盘证据与 clone 矛盾（correctness）——外部 .log 永不入库但 verify 判 MISSING 使 clone 必红；回执增内联有界尾部 evidenceTail（>4000 字节落盘面，脱敏后尾部 ≤2000 字符；summary/evidenceSha256 为既有内联面），verify 对 committed 模式外部日志缺失降级为响亮 note 而非 MISSING（local 模式 MISSING 语义不变）；篡改内联字段经 D1 字段级对账判 TAMPERED exit 2。④ D4 根 .gitignore 压死 committed 模式（correctness）——根排除 .kimi-base/state/ 时嵌套 .gitignore 捞不回（git 规则：父目录被排除即不下降），旧实现 git add 裸报 GIT_FAILED exit 1；gate 在 committed 模式前置 git check-ignore 实测探测，命中即 blockedError exit 2（EVIDENCE_GITIGNORE_CONFLICT）点名根 .gitignore 并给修法（删除排除行/设 ! 例外/改回 local）。⑤ D5 policyHash 绑本地 state 致 clone 假陈旧（correctness）——strength set 的 state 覆盖永不入库；cli.mjs 在 committed 模式对 strength set 输出显式「不随 git 入库」警告并给恢复路径，verify 的 policyHash 失配报文附偿还指引（strength set --profile 恢复一致，或重跑 gate 绑定当前策略）。顺手 info 项：state.README 从「两行 .gitignore」更新为按 evidence.mode 双形态如实描述。testing warning 低成本锁定：isEvidenceOnlyAdvance 导出并入 selftest 三条正反向用例（证据入库提交=true / 代码内容提交=false / local 模式恒 false——无条件 true 变异在两条反向面现形），selftest 24→27 项。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/receipt-v2.test.mjs 20/20；node .kimi-base/audit/run-tests.mjs 11 文件 279/279 exit 0；selftest 27/27、catalog lint、arch check --scan、adr check、spec lint、trace（PLANNED_HAS_TESTS 三条预期，不动规格）、rules-audit、sync-check、dod、manifest --check（digest 0e79dafd1e59，100 文件）全 exit 0；gate --risk high PASS=9 绑定当前指纹。
- v3.0 P4 第三轮评审修复（2026-09-08；二轮评审 verdict=FIX_REQUIRED：上轮修复引入 3 个新 error，独立测试作者追加 3 红测，红因逐条核对为缺陷本身；实现未动任何测试文件与断言）：① E1 交集对账多塞键绕过（correctness）——上轮字段级对账取共享键交集，注入账本尾没有的键（validUntil=未来）+ 重修哈希即把 STALE 洗成 exit 0（stale 循环 validUntil 分支跳过指纹比对）；verify.mjs 对账改「只能少键不许多键」：镜像多出任一账本条目没有的键即 TAMPERED exit 2 点名检查（少键仍是 v1 兼容唯一合法形态，v1 回归用例保持绿）；对账一致后 validUntil/range 豁免分支的取值才与账本条目一致，豁免永不作用于伪造字段。② E2 归档段零鉴权（correctness）——归档段内容从不被链校验：就地剔除 deferred 行或投放字典序靠后的伪造归档（内置假偿还 PASS）都静默洗白债务视图。ledger.mjs 新增 verifyLedgerHistory（归档段按文件名时间戳字典序=时序 + 当前段逐段重放：anchor 三重对账 chain==上一段链尾 / count==此前各段数据条目累计 / contentHash 自洽，保留「有归档必有 anchor、有 anchor 必有归档」双向截断检测），返回断点前的可信前缀 trusted；verify/quality×2/risk/invariants 全部改吃全史鉴权（verifyLedgerChain 保留为内存自证原语），readLedgerHistory（上轮 D2 的未鉴权全史视图）删除——债务判定只吃 trusted 前缀，伪造归档内的假偿还永不清债；risk 在归档被篡改为「剔债」形态时经 ledger-chain-broken（high）响亮非零，伪造归档形态仍报 FAST_MODE_DEBT。③ E3 evidenceTail 截断错位（correctness）——`boundedText(rawEvidence.slice(-4000), 2000)` 落进回执的是 [-4000,-2000) 中段，真实末尾（判定行）被丢弃；core.mjs 新增 boundedTail（先脱敏再截取保尾——脱敏在完整文本上做完整正则匹配，消除截取边缘的密钥碎片理论面，注释写明顺序依据），gate.mjs evidenceTail 改走 boundedTail(rawEvidence, 2000)。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/receipt-v2.test.mjs 23/23；node .kimi-base/audit/run-tests.mjs 11 文件 282/282 exit 0（harness 轮转/gaps DRIFT·BROKEN/audit dod 等旧面零回归）；selftest 27/27、catalog lint、arch check --scan、adr check、spec lint、trace（PLANNED_HAS_TESTS 三条预期，不动规格）、rules-audit、sync-check、dod、manifest --check（digest a0c124a3706a，100 文件）全 exit 0；gate --risk high PASS=9 绑定当前指纹。
- v3.0 P4 第四轮评审修复（2026-09-08；三轮评审 FIX_REQUIRED：2 error，独立测试作者追加 2 红测，红因逐条核对为缺陷本身；实现未动任何测试文件与断言）：① F1 镜像对账失锚（correctness）——镜像对账锚点（latestByCheck/historyByCheck）只来自当前段，cap=1 轮转把某 check 的账本条目全部归档后 `if (!tail) continue` 整体旁路（回滚归档旧回执/凭空伪造镜像都 exit 0）；verify.mjs 对账锚点改走 verifyLedgerHistory 的 trusted 可信全史（断链时已判 BROKEN，对账只吃可信前缀），证据重哈希仍走当前段（retention 清旧证据文件是合法面，归档段重哈希会把合法清理误判 MISSING——两个循环自此分离并注释）；v1 少键兼容与 E1 多塞键 TAMPERED 语义不变。② F2 当前段尾部截断失声（correctness）——链是前缀可截断的：删尾行后剩余链合法，纯链校验无从发现。实现 head 锚：appendLedgerRecord 每次追加（含轮转后）在写锁内经 verifyLedgerHistory 重建 `.kimi-base/state/ledger-head.json`（链尾 chain + 数据条目数 + fastDebt 快照 + contentHash，原子写）；ledger.mjs 新增 readLedgerHead/reconcileLedgerHead 单源对账（锚不可解析按伪造面处理；旧版账本无锚 = 降级 note 不谎报篡改）；verify 锚对账失败 TAMPERED exit 2 点名截断/锚/链尾；risk 在锚对账失败时用锚的债务快照与可信前缀保守并集兜底——篡改告警与 FAST_MODE_DEBT 并存（截断 ≠ 免债）；fastDebtOf 因此从 fast.mjs 被 ledger.mjs 复用（fast 不 import ledger，无环）。head 锚接入 committed 模式 git 视野（stateGitignoreContent 放行 !ledger-head.json，state.README 同步）；retention prune 只触 evidence//context/ 子目录，锚天然豁免（如实说明而非新增代码）。③ warning 顺手项：docs/PROTOCOLS.md §5 增全史鉴权与 head 锚段落，并诚实声明残余风险——无密钥本地账本只能 tamper-evident 不能 tamper-proof（决心改写者可连锚带链重写），committed 模式 + git 提交史是缓解。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/receipt-v2.test.mjs 25/25；node .kimi-base/audit/run-tests.mjs 11 文件 284/284 exit 0（harness 轮转 anchor/gaps 断链/audit dod 等旧面零回归）；selftest 27/27、catalog lint、arch check --scan、adr check、spec lint、trace（PLANNED_HAS_TESTS 三条预期，不动规格）、rules-audit、sync-check、dod、manifest --check（digest a24c5ba8495d，100 文件）全 exit 0；gate --risk high PASS=9 绑定当前指纹。
- v3.0 P4 第五轮评审修复（2026-09-08；四轮评审 FIX_REQUIRED：1 error，测试作者面在 tests/receipt-v2.test.mjs 追加 1 红测——红因核对为缺陷本身：verify exit 0 仅降级 note、risk 无声；本回合经主代理授权测试与修复同人，严格先红后绿）：① G1 head 锚缺失无新旧账本判别器（correctness）——reconcileLedgerHead 的「无锚=降级 note」对任何账本生效，rm ledger-head.json + 尾部截断 deferred 即绕过 F2 全套保护；修复：reconcileLedgerHead 增新旧账本判别器——可信全史含 v2 绑定键（policyHash/engineHash/catalogHash，REQ-053 起键恒存在）或 kind=deferred（REQ-054）条目的账本必出自带锚引擎，无锚即 TAMPERED（锚被删除 = 尾部截断灭迹）；纯 v1 旧账本（全无 v2 键且无 deferred）缺锚维持降级 note 不谎报（红测内对照段锁定，红前即绿、修复后仍绿）。② 配套债务防失声：fastDebtOf 把 fastWindow 印记的 SKIPPED 回执作为 deferred 旁证并入债务视图（每笔 deferred 都由一条 SKIPPED 产生，键集不变；deferred 尾行被灭迹时 risk 仍报 FAST_MODE_DEBT）——锚快照兜底覆盖「删尾留锚」、SKIPPED 旁证覆盖「连锚带尾」，两条灭迹路径都封死。committed 模式 stateGitignoreContent 的 !ledger-head.json 放行不变。证据（均无管道直跑退出码，2026-09-08 本机亲跑）：node --test tests/receipt-v2.test.mjs 26/26；node .kimi-base/audit/run-tests.mjs 11 文件 285/285 exit 0；selftest 27/27、dod、manifest --check（digest 559a93c06b74，100 文件）、sync-check 全 exit 0。

- v3.0 P3 强度策略引擎（2026-09-05~07；红测先行：独立作者 tests/strength.test.mjs 28 用例全红起步，逐条核对红因；实现由独立子代理完成，会话中断后由主代理收尾）：新增 lib/strength.mjs（四内置档 explore/rapid/balanced/strict × 12 封闭轴；档序单调；extends 逐轴只收紧配置期报 STRENGTH_WEAKENING；floor 四类（risk/operation/attribute/path）只升不降取最高；shadow 迁移模式只报告不阻断；policyHash 绑配置与输入；decision log 有界 ≤200 条带 policyRevision/inputDigest/reasons）+ strength list/status/set/explain 动词（接入 P2 契约注册表；list 无配置也 exit 0、status/set/explain 无配置 exit 3 治理未开启）+ task complete 的 completionMode=forbidden 执法（explore 档 exit 2，shadow 不阻断）+ 种子 .kimi-base/templates/strength.example.json（installer SEED_ENTRIES 接线）+ config.mjs 严格校验。主代理收尾发现：audit/manifest.mjs 独立实现的 SEEDS 表缺 strength 种子对（审计者与被审计者漂移，被 audit.test.mjs:338 的双实现一致性测试当场抓住——审计独立机制按设计工作），补齐后双侧 exit 0。REQ-051/052 摘 planned 标记（CHANGELOG v3.0.2）；AGENTS.md「三个 example 种子」表述改「example 种子」防计数漂移。证据：tests/strength.test.mjs 28/28、run-tests.mjs 10 文件 253/253、selftest 23/23、catalog lint、arch check --scan、adr check、spec lint、trace（100% 55/55 active，planned 13）、rules-audit（阈值 0）、manifest --check（双侧一致）、dod 全 exit 0（2026-09-07 本机亲跑）。
- v3.0 P3 评审修复回合（2026-09-07；红蓝评审 verdict=FIX_REQUIRED：2 error + 2 warning，独立测试作者追加 6 用例——4 红缺陷回归 + 2 绿防回归收紧，红因逐条核对为缺陷本身；实现修复未动任何测试文件与断言）：① D1（error）attribute floor 对象形属性声明漏网——`String({tier:'high'})` 归一成 "[object Object]" 永不命中；修复改走 loadCatalog 正规归一化（parseAttributeDeclaration 把声明解析为 {tier,reason}），读 `.tier` 比档；② D2（error）attribute floor 裸 matchesGlob 不剥 module.root——codex 系模块（root+root 内 glob）仓根相对路径永不命中；修复改 catalog.mjs 单源 moduleMatches（root 剥离/repoRooted 语义与 catalog lint 一致；catalog 存在但非法时 CATALOG_INVALID 响亮报错不再静默跳过）；③ D3（warning）customProfiles 与内置档同名静默回落内置档——validateStrengthConfig 配置期拒绝（CONFIG_INVALID exit 1 点名档名）；④ D4（warning）strength 未入 CONTRACTS 致 assertContract 查表落空旁路（未知 flag/位置越界/重复 flag 全 exit 0）——cli.mjs 新增 contractOf() 查表单源（CONTRACTS[verb] ?? STRENGTH_CONTRACT），不并表（CONTRACTS 键集被 REQ-056 现状表测试与 selftest contractCheck 双向钉死锁定 39+help）。顺手 info 项：⑤ installer isStableAsset 自用配置排除清单补 '.kimi-base/strength.json'（潜伏缺口，当前仓内无该文件无实际泄漏）；⑥ recordDecision 读-改-写并发丢条目限制写代码注释（当前调用点单进程串行，决策日志属留痕 best-effort）。证据：node --test tests/strength.test.mjs 34/34、run-tests.mjs 10 文件 259/259 exit 0、selftest 23/23、catalog lint、arch check --scan、adr check、spec lint、trace（100% 55/55）、rules-audit、sync-check、manifest --check（digest 1ae65bce052b，99 文件）、dod 全 exit 0（2026-09-07 本机亲跑）。

- v3.0 P2 REQ-068 discover 真实仓健壮性四缺陷修复（2026-09-04；红测先行：独立测试作者 tests/discover.test.mjs 5 用例先 4 红 1 绿，实现转 5/5，未改任何测试断言——唯一测试文件改动是 313 行夹具凭据字面量按 harness/audit 同款先例改拼接构造，且首次拼接点被 fitness 跨引号闭合误伤（`"abcdefgh'` 凑足 8 字符）二次校正为 `"abcdefg'+'h123"`）：① 重复模块（discover.mjs proposeModules）——单文件深层组坠入顶层兜底时 root 收窄到持有文件公共目录前缀（新增 commonDirPrefix），收窄后仍撞既有同 root 模块则 paths 显式收窄到文件子模式而非复制裸 **，收窄事实写 needsDecision（modules[].paths）；② 环不处理——新增 Tarjan 迭代版 stronglyConnectedComponents（无递归爆栈），dependsOn 图 SCC 凝聚：环内模块合并为排序首模块名，root 退公共前缀 + paths 列各成员子树（防 root+裸 ** 吞环外邻居归属），SCC 内部边消除、环外边映射凝聚节点，凝聚事实写 needsDecision（含"凝聚/强连通/SCC/环"字样，测试锁定）；③ tier 重算——最长路径分层改在凝聚后的 DAG 上计算，层向不变（tier-1=最内层）；④ fitness --all 重复报告（fitness.mjs runFitness）——按底层文件身份去重：stat dev:ino（穿透软链、硬链共享 inode）→ 失败退 realpath → 再退路径串，去重键=身份+(line,ruleId)，保留首次报告。证据：node --test tests/discover.test.mjs 5/5；run-tests.mjs 9 文件 218/218 exit 0（含 cli-contracts 16/16 无回归）；selftest 17/17、catalog lint、arch check --scan、dod（8 PASS + 1 STALE 不阻断）、scan-secrets（146 文件 0 发现）、manifest --check（digest de5070a403b1，96 文件）全 exit 0（2026-09-04 本机亲跑）。真实仓复核（/tmp/kimi-perf-calib 校准现场 dry-run，未动其任何文件）：exit 0，2197 tracked→10 提案模块，(root,paths) 重复 0、dependsOn 无环（DFS 检环 none）、tier 边违规 0、needsDecision 含凝聚记录（1 个 SCC：bridge↔buddy↔cli↔… 多模块大环凝聚为单模块）与 paths 收窄记录各 1 条。已知残余（如实记录，不阻断本验收）：凝聚模块若含 root==公共前缀的成员，其 paths 退 ['**'] 与收窄后的兜底模块在 classifyPath 同深度时可能报 overlap——discover 草案契约（无重复/无环/层序一致）不受影响，--write 落盘后 lint 调边界仍属人工批改面。
- REQ-068 追记（2026-09-04 第二回合，真实仓全链路验收驱动）：上轮回合预言的残余面在 /tmp/kimi-perf-chain 真实发生（discover --write 后 lint overlap=1503/2197=68%，凝聚模块 root=src + 裸 ** 吞并 9 模块子树），按 red-locks-the-bug 修复。根因定论：**同 root 双模块是同深度 overlap 的结构性来源**（classifyPath deepest-wins 无法仲裁同深度，glob 特异性救不了 tie），paths 收窄方案治标不治本。修复：① 兜底改递归按段细分放置（placeWithUniqueRoot），root 全图唯一成硬不变量，散在已占 root 直属的文件以逐文件列表留根（覆盖面=文件并集一寸不多）；② 凝聚 root 退公共前缀时非成员占根模块让位细分（占根方同禁裸 **，双方 glob 不相交），结构变更后实边按最终模块结构重扫一遍；③ 共享前缀下凝聚模块 paths 枚举成员子树 glob + root==前缀成员的逐文件列表，裸 ** 仅允许在 root 不含其他模块文件的场合。新增红锁用例（tests/discover.test.mjs 第 6 例：环内含公共前缀散文件成员，锁 paths≠['**'] + 枚举成员子树/散文件 + root 全图唯一），未改任何既有断言。真实仓全链路（退出码均无管道直跑）：rm 旧 catalog → discover --write exit 0（28 模块，凝聚模块 assistant：root=src、257 paths=238 子树 glob+19 散文件、无裸 **、dup root 0）→ **catalog lint exit 0（2197 路径：global 76/ignored 184/mapped 1937，overlap 0）** → arch check --scan exit 1（违规 0/新债 0，1850 条 stale=旧 222 模块人工 catalog 的已还清债）→ arch baseline --write exit 0（清 1850 stale，基线 0 条——无新债可固化）→ **arch check --scan 复跑 exit 0（违规 0/容忍 0/stale 0）**。本仓电池：discover 6/6、run-tests.mjs 9 文件 219/219、selftest 17/17、catalog lint、arch check --scan、dod（8 PASS+1 STALE）、scan-secrets、sync-check 全 exit 0；manifest digest e3d130763fc9。TODO #13 残余验收面（全链路复跑）本轮关闭。
- REQ-068 追记五（2026-09-05 第六轮，correctness 第四轮 error：specifier 仲裁保真度）：x.ts 与 x.js 并存时 `import "src/b/x.js"` 被 .js→.ts 改写候选错挂 ts 模块（归 js 报假违规/归 ts 假绿 PASS 双向实证，且与 classifyPath 归属矛盾）。修复（arch.mjs moduleForSpecifier 重构为两遍仲裁）：第一遍 provides + 精确 specifier 的 paths 仲裁，有任何命中即返回；精确全 miss 才走第二遍 specifierPathCandidates 改写候选（NodeNext 回写/扩展名补全/index 回退）——精确路径命中恒优先于改写猜测，与 classifyPath 对真实文件的归属语义一致（已写进代码注释）。评审现场 /tmp/kb-amb-o0GU 实证：arch --scan 从假绿 PASS 翻为 exit 1 点名 `undeclared-dependency] a -> js`（真阳性——catalog 只声明 a→ts，代码实际 import 的是 x.js）。附带：discover.mjs 轮次文案/needsDecision 话术与 16 轮代码对齐（info）；自证锁定新增 selftest 第 16 条（直接单测 moduleForSpecifier：并存归 js-owner/仅 ts 改写归 ts-owner/无对应文件保持 null——比子进程形态更精确，与既有 import 提取自证同款），moduleForSpecifier 随之 export。教训留痕：自证夹具注释写 `import "src/..."` 字面形态会被零依赖静态测试（spec.test.mjs 的 import 正则扫全文含注释）误判为非内置依赖——payload 注释不得携带 `import "非相对路径"` 字面量（已改述并复跑 225/225 验证）。证据（均无管道取码）：discover 12/12、run-tests.mjs 9 文件 225/225、selftest 20/20、catalog lint、arch check --scan（本仓剖面与改前逐行相同：3 warning+140 未解析）、dod（8 PASS+1 STALE）、scan-secrets、sync-check、manifest --check 全 exit 0；真实仓 /tmp/kimi-perf-chain 复验 discover --write→lint exit 0（overlap 0）→arch --scan exit 0（零新债，未解析计数稳定 3365——精确优先在链表上无 flip，印证该仓无 x.ts/x.js 并存歧义）；manifest digest 98860302bbc3。
- REQ-068 追记四（2026-09-05 第五轮；评审 escalate 后按用户"自主执行不询问"指示继续修复——决策留痕：评审轮次上限是流程闸不是质量闸，红因已核对为缺陷本身且修复方向明确，停手等人才是失职）：① 同轮多 SCC 让位崩溃（形态10，discover exit 3 TypeError）——cyclic 数组轮初快照，先处理 component 的 occupant 让位把后处理 component 的成员移出 modules、成员查找落空。修复=每轮只凝聚一个 SCC 并即重算剩余模块集（上限 16 轮，残余环仍 needsDecision 诚实兜底），occupant 语义不变；附注缺陷同修：paths 重基对 root='.' 成员会得到 rel='.' 产出 './src/...' 永不匹配破坏 pattern——公共前缀改按成员 root 段直接求（root='.' 视为零段），root='.' 成员 paths 原样保留不重基（本就仓根相对；裸 ** 在 root='.' 被 lint 禁，一律逐文件枚举）。② 裸路径 specifier 扩展名改写假绿（形态11，arch --scan 把真实违例吞成 exit 0）——moduleForSpecifier 的 paths 仲裁不做 NodeNext 改写（.js→.ts 回写此前只在 resolveRelativeImport 有），`import "src/b/x.js"`（真实 x.ts）对枚举 paths 模块一律 miss。修复=specifierPathCandidates 与 resolveRelativeImport 同口径（.js 族回写+扩展名补全+index 回退+.py 形态），paths 仲裁对全部候选匹配；仍 miss 计 unresolved，绝不因归属不确定而错挂。perf-chain 计数复核：未解析 3412→3365（47 条经改写回到正确归属=修复生效；较 paths 仲裁前的 3364 多 1 条诚实 miss——root 前缀撞但 paths 未枚举，宁计未解析不错归属），arch 仍 exit 0 零新债。证据（均无管道取码）：discover 12/12；评审现场 /tmp/kb-dotrebase-tLx3 复跑 exit 0（4 模块、dupRoot 0、无环、10 tracked 源文件唯一属主、零 './' pattern）、/tmp/kb-nodenext-0hqP arch --scan exit 1 点名 undeclared-dependency a->b（假绿修复）；run-tests.mjs 9 文件 225/225、selftest 19/19、catalog lint、arch check --scan（剖面逐行同前：3 warning+140 未解析）、dod（8 PASS+1 STALE）、scan-secrets、sync-check 全 exit 0；真实仓复验 discover --write→lint（overlap 0）→arch --scan 全 exit 0；manifest digest 892fcd44c153。
- REQ-068 追记三（2026-09-05 第四轮，correctness 复审 error：第二轮再凝聚 paths 膨胀）：再凝聚只按 member.root 重推子树 glob，丢弃成员既有 paths 枚举收窄——凝聚 {q-a,w} 时 q-a（paths 已收窄 a/**、b/**）被膨胀成 q/**，幻影吞并 occupant 散文件且 lint/arch 全绿不披露；同根因触发面 moduleForSpecifier 对裸 specifier 只按 root 前缀最长胜、不看 paths（w1→g1 错挂凝聚模块制造幻影环）。修复：① 凝聚 paths 一律沿用成员既有枚举按新公共前缀重基（member.paths ?? ['**']，仅 root==前缀且成员裸 ** 时才按共享规则逐文件枚举/保留 **）——"覆盖面=成员文件并集不多一寸"硬不变量落到再凝聚路径；② moduleForSpecifier 改 paths 仲裁（复用 moduleMatches，与 owningModule 同语义；provides 是 specifier 空间声明不做 paths 检查；root='.' 只豁免裸 **；paths=['**'] 模块逐 specifier 等价旧语义）；爆炸半径实证：本仓 arch check --scan 剖面改前改后逐行相同（3 warning + 140 未解析），/tmp/kimi-perf-chain 链路零回退（28 模块同结构、lint overlap 0、arch 直接 exit 0 零新债；未解析计数 3364→3412 如实上升——原先被 root 前缀错吞的裸 specifier 现如实计为未解析）。③ testing 复审指出上轮两处修复零测试锁定（实证删掉 25/25 仍绿）：selftest 增两条引擎自证（非行为测试，归实现者）——fitness 同 inode 双路径命中去重（临时目录硬链夹具，findings/suppressed 各 1，去重前各 2；环境不支持链接时明示 SKIPPED 不假绿）与 firstVerbToken 不吞动词（子进程真实跑 `--check manifest`，断言路由到 manifest 结果而非 help 横幅；只断言路由不绑 manifest 新鲜度，防自证与漂移耦合），selftest 17→19。红测：tests/discover.test.mjs 第 10 例（round2RecondenseFixture，测试作者交付，未改一行）。证据（退出码均无管道直跑）：discover 10/10；评审现场 /tmp/kb-round2-PGoE 复跑 exit 0（4 模块，dupRoot 0/无环/无幻影；边归属如实：q-a→w、w→src(occupant)、src→ext）；四不变量逐 draft 实跑自查（root 唯一/无环/10 个 tracked 源文件唯一属主/边归真实属主）全过；run-tests.mjs 9 文件 223/223、selftest 19/19、catalog lint、arch check --scan、dod（8 PASS+1 STALE）、scan-secrets、sync-check 全 exit 0；真实仓复验 discover --write→lint→arch 全 exit 0；manifest digest 9e06add2710f。残余说明：凝聚振荡病态配置仍以 4 轮上限 + needsDecision 诚实声明兜底；moduleForSpecifier 对无扩展名的路径形裸 specifier 按 paths 精确匹配（不带扩展名解析回写）—— miss 计入未解析而非错归属，方向正确。
- REQ-068 追记二（2026-09-04 第三回合，结构化评审 verdict=FIX_REQUIRED 驱动，correctness lens 3 error + testing lens 补红测）：第二回合的让位细分+重扫实现被独立红测（tests/discover.test.mjs 形态6/7/8，复刻评审现场 /tmp/kb-resplit-*、/tmp/kb-rootdot-*）抓出三缺陷，红因均核对为缺陷本身。修复方案（采纳评审首选+自定死角仲裁语义）：① **arch.mjs owningModule 改 paths 仲裁**（根因2/4 源头修）——只有 paths 枚举覆盖的模块才算属主，root 前缀最深者胜不变；root='.' 模块从"整体跳过"收窄为"只豁免裸 **"（与 lint catch-all 禁令同形），枚举根模块的实边不再静默丢失（形态7）；catalog.mjs moduleMatches 导出复用（repoRooted 仓根 glob 与 codex 系 root 内 glob 同一覆盖语义，无循环依赖）。行为保持论证：paths=['**'] 模块与旧 root 前缀语义逐文件等价（discover 默认与绝大多数 catalog）；本仓 repoRooted catalog 与 /tmp/kimi-perf-chain discover 产 catalog 逐文件归属不变（arch --scan 违规剖面逐行相同）。② **让位死角仲裁语义定案**：occupant 散文件就住在被占 root 直属、无更深目录可细分时，上移到最近空闲祖先 root + paths 重基为逐文件列表（root 唯一不变量不破坏、归属由 paths 精确表达）；否决"同 root 共存+paths 互斥"——测试硬断言 root 全图唯一，且同 root 对 owningModule tie-break 永远是隐患；祖先全被占的病态情形留原 root 精确列表兜底。③ **凝聚—让位—重扫改 ≤4 轮循环**（根因3：重扫暴露占根模块内部 import 图碎块化新环时继续凝聚），occupant 排除已凝聚模块（防振荡），4 轮未收敛不假装无环、残余环写 needsDecision。④ fitness 抑制留痕按同一 dev:ino 身份键去重（warning 级双报，/tmp/kb-fit-* 复现验证：suppressed 行 2→1）。⑤ `--check manifest` 被 firstVerbToken 吞 verb 静默 help（quality/waiver 契约把 check 注册为 value flag）：一行修——value flag 的值恰好是已注册动词时不跳（info 级，已修）。testing lens warning 组评估：3 节点链式环/多 SCC 已由形态7 夹具隐含覆盖（src↔lib 与 app/q 两 SCC 并存+让位），重复模块覆盖由形态8 ownersOf 唯一归属断言正面锁定，contractCheck 失败路径属 cli-contracts 作者面——均不另补，理由如上。证据（退出码均无管道直跑）：tests/discover.test.mjs **9/9**（形态6 边归属正确+无环+root 唯一、形态7 root='.' 凝聚保留 →ext 实边、形态8 让位分支红锁）；评审现场独立复跑 /tmp/kb-resplit-u6aO（3 模块/dupRoot 0/无环/2 边）与 /tmp/kb-rootdot-2iph（4 模块/dupRoot 0/无环/边非零）全 exit 0；run-tests.mjs 9 文件 **222/222**、selftest 17/17、catalog lint、arch check --scan（违规剖面与改前逐行相同）、dod（8 PASS+1 STALE）、scan-secrets、sync-check、manifest --check 全 exit 0；真实仓复验 /tmp/kimi-perf-chain：rm+discover --write exit 0（28 模块与上轮逐字节同结构）→ catalog lint exit 0（overlap 0）→ arch check --scan **直接 exit 0**（违规 0/新债 0/stale 0，上轮已清基线）；manifest digest ec99e9d0c0cd。
- v3.0 P2 REQ-056 CLI 契约注册表（2026-09-04，ADR-0011；红测先行：独立测试作者 tests/cli-contracts.test.mjs 16 用例先红后绿，未改任何测试断言）：新增 lib/cli-contracts.mjs 单源注册表（Object.freeze 深冻结；39 dispatch verb + help 各一条 {usage, positional:{min,max}, flags{name:{kind:value|boolean}}, conflicts?}；GLOBAL_FLAGS={project:value, help:boolean}；flag 名/kind 逐条对账原 KNOWN_FLAGS 与 dispatch 实际消费；manifest write/check 互斥入 conflicts）。core.mjs parseCliArgs 按 valueFlags 集合解析：boolean/未知 flag 不再吞后续裸 token（裸 token 归 positional），重复 flag 记 duplicates、`--flag=` 空值记 emptyValues，返回形状 {positional,flags,duplicates,emptyValues} 向后兼容（唯一消费方 cli.mjs）。cli.mjs 删除 KNOWN_FLAGS 字面量表改契约单源派生；dispatch 前按序校验（未知 flag 列合法集→重复点名→空值点名→conflicts 互斥（文案仍含"互斥"）→位置参数上下界点名越界 token），全部先于项目根解析；verb 定位两遍扫描（首遍按全契约 value flag 并集跳过 flag 值，次遍按该 verb 契约精确解析），`-h`/`--help`/hook stdin/全部既有错误文案不变；HELP_VERBS 补 waiver 条目。selftest 增 contractCheck 项：读 cli.mjs 源码提取 case 路由集与 HELP_VERBS 键集，断言与 CONTRACTS 键集双向相等+help 全覆盖（16→17 项）。FRAMEWORK-MANIFEST 同批再生成（digest 6b98af3b00b0，96 文件）。证据：cli-contracts 16/16、selftest 17/17 exit 0（contractCheck 路由 39/契约 39/help 39）、catalog lint exit 0、arch check --scan exit 0、其余 8 个既有测试文件逐文件 exit 0（audit/gaps/harness/memory/review/scale/spec + cli-contracts，2026-09-04 本机亲跑）。偏差如实记录：run-tests.mjs 与 dod 当前被并行进行中的 REQ-068 红测拖累（tests/discover.test.mjs 4 用例红=discover SCC/tier/fitness 去重实现未落地，属另一实现者范围；dod fitness 步命中该文件 313 行夹具凭据字面量），与 REQ-056 改动无因果（discover.mjs/fitness.mjs 不 import parseCliArgs/cli-contracts）。
- v3.0 P0 基线消化（2026-09-04）：①TODO #7 关闭——真实仓性能校准（decompiled Claude Code 源：2196 tracked 文件/38.7 万行/222 模块/2299 真实 import 边，WSL2+Node24 原生 ext4）：catalog lint 257ms / impact 181ms / arch scan 1644ms / fitness 1480ms / gate(4 检查) 843ms / context pack 197ms / discover 2555ms（3 次取中位）；结论=成本驱动是文件数×模块数而非行数，60 万行设计目标成立余量大，外推边界与校准现场（/tmp/kimi-perf-calib）已入 LARGE-REPO-GUIDE §5 与 NFR-002；校准同时发现 discover 三缺陷+fitness 重复报告→立项 REQ-068 与 TODO #13。②TODO #9 关闭——插件 TUI 真实实装 E2E 全 PASS（隔离 KIMI_CODE_HOME，`/plugins install` 真实路径）：managed 副本+installed.json enabled、info 零 diagnostics、sessionStart skill 注入确认、3 个 hook 事件真实触发（SessionStart 写 session.json/PreToolUse 拦截 shutdown 记 gate-log/Stop completion-gate block strikes=1/3）、/kimi-base:status 已注册；限制如实记录：F1 `kimi -p` 不拦截斜杠命令（安装只能 TUI）、F2 SessionStart hook stdout 横幅在 -p 下不进模型上下文（P8 设计输入，TODO #14）、F3 仅标记项目首验摩擦。③TODO #10 关闭——AGENTS.md 规则逐条绑定（9 enforced/9 prompt-only/0 无执法，执法率 16.7%→50%），harness.json 设 rulesAudit.maxUnenforced=0 转门禁并红锁验证（注入无执法规则 exit 1、还原 exit 0；首次注入因含"提示词"字样被 PROMPT_ONLY 吞掉、第二次因 <25 字符被过滤，第三次才有效——红锁本身也校准了判定边界）。④文档漂移三修：ARCHITECTURE supervisor 448→454 行；REQ-009 ≤180→引擎契约 500 硬顶/220 警告（CHANGELOG v2.0.2）；CROSS-POLLINATION hook "30s"→kimi.plugin.json 实测 5–10s。证据：spec lint/trace/sync-check/rules-audit 全 exit 0（2026-09-04 本机亲跑）。
- v3.0 P1 规格与计划（2026-09-04）：Product-Spec v3.0.0（REQ-051~068 立项，17 条 planned + REQ-067 active）+ CHANGELOG v3.0.0 + ADR-0008（强度策略引擎）/0009（Receipt v2+贷款账本+可提交证据）/0010（feedback 引擎化）/0011（CLI 契约注册表+planned 标记）+ DEV-PLAN v3.0 Phase 表（P0~P10）。REQ-067 引擎落地（红测先行：独立测试作者 7 用例先红，确认红在特性缺失后实现转绿）：scan.mjs planned 标记解析（PLANNED_NO_PHASE/块在下一个其他 REQ id 处截断防跨条目渗漏——首版截断误伤验收行自引用，判定收窄为"其他 id"后修复）+ trace 排除 planned 出分母（零 active 空真 1）+ PLANNED_HAS_TESTS 警告 + CLI `trace 摘要：{...}` 机器可读投影。规格文本三处自指陷阱修复（版本行/节引言/REQ-067 自身的字面标记会把相邻 REQ 卷进判定块）。证据：run-tests.mjs 7 文件 exit 0、selftest 16/16、catalog lint、arch check --scan、adr check（11 活跃 0 幽灵）、manifest --check（digest 546df31b21e0 起）、doctor、pack-check、spec lint（68 条 0 error）、trace（100% 51/51 active，planned 17 单列）、rules-audit（阈值 0）、sync-check、dod（9 步 PASS）、gate（PASS=4 绑指纹 88bc03884071）、task complete exit 0（2026-09-04 本机亲跑）。

- CI 双红修复（2026-09-03，三轮定案）：main 连续红（runs 33707711135/33709508709/33713352964）。Round 1（f5f8acd）只给测试清理加 rmSync 重试——CI 复跑证明不够。Round 2/3 在本地 Windows（pwsh 7.6.5 + Node 24，WSL interop 真机复现率约 2/5）追到**真 bug 与一类环境竞态**：① **supervisor stop 在 Windows 挂死**（真产品 bug）：killTree 的 `spawn('taskkill')` 未 unref，ChildProcess 句柄钉住事件循环，cmdStop emit 完结果后进程永不退出——spawnSync 30s ETIMEDOUT，挂死进程持有临时目录即 CI 上的 EBUSY rmdir；修复=taskkill spawn unref+吞 error、cmdStop 成功路径 write 回调里显式 process.exit(0)（不指望事件循环排空）。本地复现还把 Windows 机器打到线程耗尽（每轮 repro 泄漏守护+服务进程链），已清理。② ENOTEMPTY 断源：git 提交派生的后台 gc/maintenance 异步补写 .git，7 个测试文件 git() helper 统一注入 `GIT_CONFIG_COUNT=2 gc.auto=0 maintenance.auto=false`。③ 清理钩子保留重试（30×500ms）+ 最终失败 t.diagnostic 留痕不留红（Defender 类短时持锁，清理失败不伪造测试结果）。FRAMEWORK-MANIFEST 已随 supervisor.mjs 载荷变更重新生成（digest 87f5637b46bc）。未动任何测试断言（Pinned 第 7 条）。证据：killTree 最小复现定位到泄漏 Socket 句柄、修复后 stop 85ms 退出（原永久挂死）、start/stop 20 轮循环全绿、Linux run-tests.mjs 190/190 exit 0、Windows 本地盘 gaps+harness 4/4 连跑全绿（修复前 2/5 红）、Windows 无进程泄漏残留；CI run 33718196067 四 matrix 全绿（ubuntu+windows × node20/22）。
- v2.0.0 提交推送 + hooks 首次实战 + TODO #8 Windows 真实环境验证关闭（2026-09-03，pwsh 7.6.5 + Node 24 亲跑）。提交 2a65c2d 已推送 origin/main；本仓 hooks 挂载后首 commit 即被第二道闸实拦两次并修复：scan-secrets 拦未拼接构造的测试金丝雀（tests/gaps.test.mjs:408）、catalog lint 拦 .gitattributes 未入 module-catalog；顺手修 pre-commit run_step 退出码捕获 bug（$? 取到 if 语句而非命令，报「失败（exit 0）」）。pre-push dod 9/9 PASS + gate PASS 放行推送。pwsh 7 真实环境验证：setup.ps1 安装临时仓 exit 0（90 文件+四种子）；doctor installed 模式正确区分用户定制与未挂载警告；**发现真 bug**——已安装项目用自带引擎重跑 `install . --hooks` 被 UNSAFE_TARGET 误拒（旧哨兵只看目标与引擎根重叠，而这条正是 doctor 打印的补救路径）；修复=区分器改为源根 kimi.plugin.json + example 种子源纳入受管载荷（否则已安装项目重跑 install 报 SOURCE_SURFACE_MISSING；audit/manifest.mjs 独立实现的 digest 分歧被独立性测试当场抓到）；回归测试 +2（已安装自重跑挂载 core.hooksPath / 源仓自我安装仍拒）。从 pwsh 调用 git commit 触发 sh 钩子全链实测：干净提交放行、真实 ghp_ 机密字面量提交被 scan-secrets 拦截 exit 1；githooks/ 与 skill-builder scripts/ 增目录级 .gitattributes（LF 锁定防 autocrlf 弄坏 sh）。证据：run-tests.mjs 190/190、selftest 16/16、catalog lint、manifest --check（digest 3398782154ed，95 文件）、doctor、dod 全 exit 0。
- P7c（v2.0）文档、ADR 与 2.0.0 版本切。新增 docs/adr/0002~0007 六条（三面执法 / 评审引擎化 / privacy 保护底线 / 源布局=安装布局 / 退出码契约 v2 / fleet 采纳含回滚条件；格式承 0001，Enforced-by token 全部经 adr check 实测解析——0002 采用 manifest-check+pack-check 而非裸 manifest，后者实测为幽灵引用）。README 重写（三面执法为头条、新布局图无 template/、40 动词速查表按 help 实测重排、install --hooks、诚实边界保留 fail-open/启发式/本地账本三声明并补 git hooks 无 node 响亮 fail-open）。ARCHITECTURE 更新到 lib/ 31 模块子系统地图 + 三面执法层 + 状态目录实况（waivers/arch-trend/review·review-backlog 等），动词数按 cli.mjs case 实测 39 分支 + help = 40（纠正旧"25 动词"宣称）。CAPABILITY-MATRIX 更新（>6k 行拆分触发行已兑现移入「v2.0 已兑现的升级项」；新增评审/记忆/需求治理/三面执法/fleet/cochange/budget 行；CI 行改指 templates/github-gate.yml）。CROSS-POLLINATION 升七仓台账：新增 dsh-base 专项（吸收 14 条逐条落点；刻意不搬 6 缺陷——DIFF_EXCLUDED 自我陈旧化/backlog 随会话冲掉/非终审 ACCEPT 出证据/单词正则 waiver/dod·release 清单双份/record-to-green 基线，kimi 对策均经代码核对：git.mjs 逐文件内容摘要指纹、paths.mjs backlog 独立常量、review.mjs `verdict==='ACCEPT' && final` 才写回执、hygiene DOD_STEPS 单源 release 直接 import、arch trend 逐指标历史最优；行业实践拒绝 6 条各一句理由）。新建 CHANGELOG.md（v2.0.0 Added/Changed/Fixed + v1.0.0 追溯）。版本切 2.0.0：package.json / kimi.plugin.json / core.mjs TOOL_VERSION（help 横幅实测输出 kimi-base/2.0.0；setup.sh/ps1 无版本引用；tests 内 1.0.0 均为 fleet 契约夹具非工具版本）；package.json files 增补 CHANGELOG.md。DEV-PLAN 追加 v2.0 P1~P7c 阶段表（各带证据指针）。LARGE-REPO-GUIDE §5 追加 P7a 实测冒烟表（合成仓 4954 文件/151 模块：lint 332ms/impact 227ms/scan 1373ms/gate 518ms）并写明诚实外推边界（合成仓仅约 1 万行，60 万行校准仍挂 TODO #7）。TODO 前移：#7/#8/#9 保持 OPEN；#10 更新为复测值 16.7%（18 条：enforced 3/prompt-only 2/无执法 13；v2.0 前 11.8%）。证据：`node .kimi-base/audit/run-tests.mjs` 188/188（7 文件 exit 0）、selftest 16/16、catalog lint、arch check --scan、adr check（活跃 7/幽灵 0）、manifest --check（digest e91608bdede5a952，tool=kimi-base/2.0.0）、doctor .、pack-check（123 文件）、spec lint（50 条 0 error 0 warning）、trace（100% 50/50；REQ-101←模板 1 处文档悬空仅报告不拦，既有行为）、dod（9/9 PASS）、sync-check、rules-audit（建议态）全 exit 0（2026-09-02，Windows + Node 24 本机亲跑）。未提交：git status 101 条（v2 全程未提交为既定状态，commit 权属用户）。
- P7b（v2.0）dogfood 缺陷双修（关闭 TODO #11/#12，语义定案见 Decisions 末两条）。Fix 1 fast 借账：completionGate 不再接受 fastWindow 印记的 SKIPPED（quality.mjs——任何 fast 借账回执一律记缺口并点名还债路径「fast off 后重跑完整 gate」；waiver 覆盖 BLOCKED 不变；runtime 时间窗回执不变）；`fast status`/`fast on`/help 明文「fast 门不能关闭 task/release」；hooks.mjs 横幅与 memory.mjs invariants 经核对本就未陈述旧规则（「skip 是欠账不是豁免」早已在册），未改。Fix 2 stale≠完整性失败：hygiene.mjs classifyStepExit 新增第四态 STALE（exit 4 归级 STALE，响亮带输出尾部但不阻断 dod；exit 2 篡改/断链/缺失/漂移仍 FAIL）；release.mjs ledger-intact 判据收窄到 problems 面（与其注释一致，stale 计入细节但不阻断）；release.mjs receipt-fresh 与 verify.mjs stale 分级同时认 range 评审回执（kind:review 且 range.head===当前 HEAD 即 fresh，HEAD 移动才 stale）。回归测试 +6（harness fast 借账 2、audit dod STALE/篡改 2、scale release 2）：fast on→gate SKIPPED→complete exit 2 点名 fastWindow+还债路径→fast off→完整 gate→complete exit 0；陈旧回执 dod exit 0 且 STALE=1 可见；篡改证据 dod exit 2 点名 FAIL receipt-verify；工作树评审 ACCEPT→提交→gate→dod exit 0→release READY（P7a 卡死场景全程翻绿）；range 评审 ACCEPT 后脏树 verify exit 0 + release receipt-fresh [x]，HEAD 移动后 verify exit 4 点名 range.head。文档同步：rules/workflow.md Fast Mode 节、PROTOCOLS §4 绑定规则/§8 fast-mode/§11.5 评审回执新鲜度/§15.5 release 表+职责划分段、OPERATIONS dod/review/fast/receipt verify/release 各行、plugin/commands/fast.md 第 4 条。dogfood 复跑（一次性脚本 /tmp 未进仓；夹具两轮修正：种子 harness 的 quality.riskChecks 配种子矩阵不可单换矩阵、安装载荷被 trace 扫描须在夹具 spec 声明 NFR-001）：install exit 0 → configure+commit → 工作树 review start/blue/lens/verdict 终审 ACCEPT 写回执 → commit → receipt verify exit 4（stale-only 链完好）→ gate PASS → dod exit 0（PASS=8 STALE=1，receipt-verify 响亮可见）→ **release exit 0 READY 八项全 [x]** → fast on 后 gate SKIPPED=1 → task complete exit 2 点名借账 → fast off 完整 gate 后 complete exit 0。证据：`node .kimi-base/audit/run-tests.mjs` 188/188（7 文件）、selftest 16/16、catalog lint、arch check --scan、manifest --check（digest 0f97f02b4a06）、doctor .、pack-check（116 文件）、spec lint（50 条）、trace（100% 50/50）、dod（9/9 PASS）、sync-check 全 exit 0（2026-09-02，Windows + Node 24 本机亲跑）。
- P7a（v2.0）测试补盲 + Spec 需求补立 + 端到端 dogfood + 性能冒烟。新增 tests/gaps.test.mjs 16 例，覆盖此前零覆盖引擎面：pre-write 拦截三面（仓外写入/敏感文件 .env·id_rsa/task-conflict 熔断——owned 路径被任务外改动 exit 2 且 gate-log 记规则，基线一致写入认领放行；对账降级面 harness.test.mjs 已覆盖不重复）；receipt verify 证据面（证据文件篡改 TAMPERED/删除 MISSING/镜像自洽但偏离账本尾 DRIFT/账本中段篡改 BROKEN，全 exit 2；轮转 anchor 面 harness 已覆盖）；retention prune（--dry-run 只报不落盘、过期证据真删、receipt 引用证据保护）；risk scan（干净仓 exit 0、active 任务 >72h 标 stale-task、同检查连 3 FAIL 标 fail-streak exit 2）；supervisor 生命周期（start 确认 liftoff/status/stop 收敛收割子进程）与重启风暴熔断（maxRestarts=2 窗口内超即 crashed）；gate 检查形态（builtin:fitness 出 PASS 回执 argvDisplay=builtin:fitness、未知 builtin 配置期 exit 1、platform 不匹配 BLOCKED exit 2+本平台 PASS+非法值 exit 1、resourceLocks 跨进程互斥——活进程持锁则 gate 等锁超时 exit 1、释锁后通过且锁文件无残留）。Product-Spec.md 新增 REQ-036~044（P5 三面执法：git hooks 电池 install --hooks/审计脚本独立/dod 静态电池；P6：catalog discover/cochange/budget/fleet/privacy 底线/release），Product-Spec-CHANGELOG.md v2.0.1 同改，tests/audit.test.mjs 与 tests/scale.test.mjs 头部接追溯锚点（trace 100% 50/50）。dogfood（一次性脚本在 OS 临时目录跑 2 模块小仓，31 步全实录未进仓）：install --hooks exit 0（core.hooksPath=.kimi-base/githooks、三钩子 100755 入 index、载荷与四种子落地）→ 配置 matrix 单测+2 模块 catalog 并提交载荷（pre-commit 电池首考通过；budget 超支按设计 ADVISORY 响亮不拦）→ task start(medium)→gate(PASS=4)→quality status→task complete 全 exit 0 → review personal 剖面 pack/start/blue(stdin claims)/lens correctness(stdin findings)/verdict 终审 ACCEPT exit 0 且回执写入 receipts 镜像（lenses=correctness）→ fast on 后 gate SKIPPED=1（非 protected 被跳过）→ 窗口内 task complete 实测 exit 0（引擎既定语义，记 TODO #12）→ 同刻 release exit 2 点名 fast-mode-closed+fast-debt-repaid（债在发布层拦截实锤）→ fast off 完整 gate PASS=4 后 complete exit 0 → 改一字节 receipt verify exit 4 → pre-commit 拦机密字面量提交（exit 1 scan-secrets 点名）+干净提交（代码+progress.md 同改）过电池 → 提交后重跑 gate 补 fresh 回执 → release exit 2 诚实 NOT READY（阻断：dod-static——spec-lint/trace 对无 Product-Spec 仓如实 DEGRADED 不假绿、ledger-intact+receipt-verify FAIL——历史评审回执 stale，根因记 TODO #11）→ doctor exit 0。性能冒烟（合成仓 4954 文件/151 模块，Windows+Node 24）：catalog lint 332ms / impact --git（改 core 触发最大反向闭包）227ms / arch check --scan 1373ms / gate（2 琐碎检查）518ms，全 exit 0；对照 LARGE-REPO-GUIDE 目标（60 万行/64 模块 lint<10s、impact<5s）余量两个数量级，但合成仓仅约 1 万行，60 万行真实校准仍挂 TODO #7，不外推吹牛。本阶段零载荷改动（manifest digest c4a61a4dc963 不变）。证据：`node .kimi-base/audit/run-tests.mjs` 182/182（7 文件；gaps.test.mjs 新增 16）、selftest 16/16、catalog lint、arch check --scan、manifest --check、doctor .、pack-check（116 文件）、spec lint（50 条 0 error 0 warning）、trace（100% 50/50）、dod（9/9 PASS）、sync-check 全 exit 0（2026-09-02，Windows + Node 24 本机亲跑）。

- P6（v2.0）规模化与治理深化六件套：`catalog discover [--write] [--depth N]`（lib/discover.mjs 移植 dsh discoverCatalog：源码目录分组≥2 文件成组+顶层兜底、真实 import 边经 arch scanRealEdges 探针推导 dependsOn、tier-N 位置分层、构建清单命令检测、生产源码属性信号提案封顶 high、猜不了的进 needsDecision；已有 catalog 时 --write 写 *.draft.json 绝不覆盖；init-modules 保留为废弃别名转发并 stderr 注明）；`cochange`（lib/cochange.mjs 移植 dsh coChange：git log --no-merges 共变耦合、>8 模块横扫提交排除并计数、accepted 三元组降级 warning、BOUNDARY_SUSPECT exit 1、<30 有效提交 LOW_CONFIDENCE exit 0、零共变模块列抽仓候选；catalog 新增 cochange 段严格校验）；`budget [--staged|--baseline ref]`（lib/budget.mjs 移植 assessBudget：四指标对照 harness.json budget 段严格校验全可选正整数、超限 exit 1 固定话术"超出预算意味着拆分变更或升级——永不靠放宽预算消红"、未配置 exit 3；pre-commit 末尾 ADVISORY 接入 `budget --staged || true`）；fleet 全量移植（lib/fleet.mjs：fleet.json 经 --fleet/KIMI_BASE_FLEET/祖先查找定位，lint 20 类违例码、impact BFS+coordinationCost、status 逐仓 spawn doctor 退出码语义+--deep 加 dod、recap 逐仓前 5 dash 行总量封顶；config.mjs 新增 KIMI_BASE_ROOT 钉根；新增 templates/FLEET.md+fleet.example.json 仅作文档模板不种子）；privacy 提升保护底线（PROTECTED_ATTRIBUTES={security,safety,privacy}、waiver 禁词含 privacy/pii/隐私/个人信且覆盖 reason/compensation 文本、matrix allowFastSkip 配置期双通道拦截、fast 运行期免疫、harness 两份 governance.protected、Pinned 第 3 条用户拍板修订、全 payload/docs 旧表述清扫）；`release` composite（lib/release.mjs：静态电池复用 hygiene DOD_STEPS 单源+fast 窗口关闭+fast 欠账已还（最新指纹批次无 fastWindow/SKIPPED）+账本链完好+当前指纹 fresh 回执+sync-check 干净+backlog 无过期为阻断项、risk scan 建议项；exit 2 逐项列缺口；明示永不打 tag/push/建分支）。文档：AGENTS.md 速查、OPERATIONS 第 3/11 节、PROTOCOLS 第 15 节、LARGE-REPO-GUIDE 棕地四步+共变复审节奏、large-repo-harness SKILL 换 catalog discover。证据：selftest 16/16、`node .kimi-base/audit/run-tests.mjs` 166/166（tests/scale.test.mjs 新增 22：discover 3/cochange 5/budget 3/fleet 5/privacy 3/release 3）、catalog lint、arch check --scan、manifest --check（digest c4a61a4dc963）、doctor .、pack-check（116 文件）、spec lint（41 条 0 error）、trace（100% 41/41）、dod（9/9 PASS）全 exit 0；catalog discover 本仓 dry-run exit 0（86 tracked→3 模块提案）；cochange 本仓 exit 0 带 LOW_CONFIDENCE（1 提交，薄历史如实）；budget exit 1 真实超支（v2.0 全程未提交：changedFiles 124>80，信号诚实——应拆分或升级）；release exit 2 唯一阻断 receipt-fresh（本仓从未跑过 gate，真实缺口）（2026-09-02，Windows + Node 24 本机亲跑）。

- P5（v2.0）三面执法层：git hooks + 独立审计脚本 + CI 门禁（移植 dsh ADR-0002：执法住在 git 与 CI、fail-closed）。新增 `dod` 组合动词（lib/hygiene.mjs DOD_STEPS 唯一事实源，P6 release 复用；九步静态电池子进程跑真实 CLI：catalog lint→skills-lint→agents-lint→spec lint→adr check→fitness --all→trace→receipt verify→arch check；任一 FAIL exit 2、仅降级 exit 3、4=STALE 按 FAIL）；fitness 新增 `--staged`（暂存区）与 `--all`（tracked∪未跟踪，与 trace 同口径）。`.kimi-base/githooks/` 三钩子（纯 POSIX sh）：pre-commit 跑 scan-secrets/scan-instructions/check-syntax --staged + catalog/skills/agents-lint + fitness/sync-check --staged（首个失败即拦，降级响亮不拦）；pre-push 跑 dod+gate（非零含 3 即拦）；commit-msg <12 字符/废话黑名单拦截、>72 警告、Merge/Revert 豁免；无标记静默、无 node 响亮 fail-open。`.kimi-base/audit/` 五脚本（零依赖、禁 import 引擎、stdout 单行 JSON）：scan-secrets（禁 tracked 路径+五类凭据形状+放行上下文+逐行抑制）/scan-instructions（8 注入规则类，secret-file-read 为 warning）/check-syntax/manifest（独立重算 LF-sha256+digest，与引擎 digest 逐字一致 5d4d0fc793d7）/run-tests（Node 20 安全启动器，无测试 exit 3）；独立性由 catalog `audit` 模块 forbiddenDependencies:["engine"]（arch --scan 执法）+ 静态测试双重锁死。installer：MANAGED_ENTRIES 加 audit/githooks，`install|upgrade --hooks`（core.hooksPath+chmod 755+git add --chmod=+x；非 git 响亮降级不回滚）；doctor 对已安装 git 仓警告"第二道闸未挂载"。package.json files 加两载荷目录、test 脚本改走 audit/run-tests.mjs（单一事实源）；ci.yml 重写为 selftest→四独立审计→run-tests→dod→arch trend --gate（ubuntu+windows×node20/22）；新增采纳者模板 .kimi-base/templates/github-gate.yml（installer 不写 .github）。文档：OPERATIONS 第 10 节、ISOLATION-PROFILES 第 5 节三面模型、AGENTS.md 载荷/速查/目录。证据：selftest 16/16、`node .kimi-base/audit/run-tests.mjs` 144/144（harness 55/review 27/memory 13/spec 20/audit 29 新增）、catalog lint、arch check（含 --scan）、manifest --check（引擎+独立双实现一致）、doctor .、pack-check、spec lint、trace、sync-check、dod（9/9 PASS）全 exit 0；另在提交态副本（tar 拷贝+git init+commit）模拟 CI 序列全绿：check-syntax 38 文件、scan-secrets 123 文件 0 发现、scan-instructions 27 指令文件 0 发现、run-tests、dod、arch trend --gate（2026-09-02，Windows + Node 24 本机亲跑）。

- P4（v2.0）记忆法动词 + 需求可判定性/追溯：新增 `lib/memory.mjs`（recap 派生预算视图——Position 现算不信摘要、总量含截断注记 ≤ 预算、缺 progress.md exit 3；invariants ≤1200 字符铁律+实时状态；archive 默认 dry-run、移动最旧 Done/Notes 留指针、归档只增不删；sync-check 执法 MEMORY_BEHIND_CODE/SPEC_WITHOUT_CHANGELOG，非 git 无 --paths exit 3）与 `lib/scan.mjs`（spec lint 八类可判定性检查/trace 覆盖门禁+悬空引用+对称规则只扫 REQ·NFR 声明族/spec view 预算摘要省略显式点名/rules-audit 三态执法率默认纯建议/skills-lint/agents-lint）。sessionStart 横幅默认注入 invariants（`hooks.injectInvariants`）；harness.json 新增 `spec`/`rulesAudit` 严格校验段（含 example 种子）；`/kimi-base:recap` 改走引擎 recap+invariants；skill-description-lint.sh 删除由 `skills-lint` 取代（test-skill-behavior.sh 保留）；Product-Spec.md 41 条需求全部改写为可判定形式（规范词+触发+验收行）并新增 REQ-031~035（review/记忆法/spec·trace/rules-audit/资产 lint），CHANGELOG v2.0.0 同改。证据：selftest 16/16、`node --test tests/harness.test.mjs tests/review.test.mjs tests/memory.test.mjs tests/spec.test.mjs` 115/115（harness 55 含新增 impact/pack-check、review 27、memory 13、spec 20）、catalog lint、arch check（含 --scan）、manifest --check、doctor .、pack-check、fitness、recap、invariants、spec lint（41 条 0 error 0 warning）、trace（覆盖率 100% 41/41）、rules-audit（建议态，执法率 11.8%）、sync-check 全部 exit 0（2026-09-02，Windows + Node 24 本机亲跑）。

- P3（v2.0）结构化对抗评审引擎化：新增 `lib/review.mjs`（会话 state/review/session.json、九 lens 三阶段库、四剖面+属性收缩选拔、blue/lens/verdict/status/team/backlog/pack 八子命令）+ catalog `review` 段严格校验（profile/lenses/maxRounds 1..10/requireStructured）+ 完成门接线（review 段且 requireStructured≠false 且 risk=high → 需 fresh 终审 ACCEPT 回执）+ risk scan 标记过期 backlog。修复 dsh 三个已知缺陷：location 正则兼容 Windows 路径（锚定结尾行号）；`--ad-hoc` 额外证据通道（非召集 lens 可上报、不受阶段门控、error 仍压裁决）；backlog 独立 `state/review-backlog.json` 持久（重开评审不冲掉）。裁决前沿模型（completeThrough/frontier）使非终审 ACCEPT 可达（dsh 中 final:false 为死代码）；回执只在终审 ACCEPT 写账本，消费者只认回执。red-blue-review skill 重写为引擎编排（删除 red-blue-review.sh 与 RED-BLUE-REVIEW.md——职责分别被 review pack 与会话/账本取代）。证据：selftest 16/16、`node --test tests/harness.test.mjs tests/review.test.mjs` 80/80（新增 27）、catalog lint / arch check（含 --scan）/ manifest --check / doctor . / pack-check / fitness 全 exit 0（2026-09-02，Windows + Node 24 本机亲跑）。

- P2（v2.0）契约硬化与继承缺陷清算：退出码契约 v2（0 通过 / 1 用法·违规 / 2 阻断 / 3 降级·引擎错误 / 4 陈旧证据；hook 保持 0/2）；全动词严格 flag 校验（未知 flag exit 1 列合法集）；arch trend 棘轮改逐指标历史最优（debt-swap 净零回弹必拦）；runtime 类证据（class:"runtime" + validUntil/time-window-<N>h）；账本 retention.ledgerMaxEntries 轮转 + anchor 跨段续链；outputLimits.hookChars/modelChars 接线；MARKER_DIR 删除；配置别名统一（stopMaxBlocks/signalKeywords/budgetTokens 为 canonical）；prewrite 对账腐化降级响亮留痕；pre-compact 写入上文件锁；gate-audit 已知闸派生自分类器规则表（补 6 条漏网）；supervisor 用法错误 3→1；分类器 git 长选项前缀匹配 + docker/podman --env-file 外发面；PROTOCOLS/large-repo/OPERATIONS/state.README/skills 文档漂移清扫。证据：selftest 16/16、`node --test` 53/53、catalog lint / arch check / manifest --check / doctor . / pack-check / fitness 全 exit 0（2026-09-02，Windows + Node 24 本机亲跑）。

- P1（v2.0）结构重组与自托管：template/ 消亡，载荷迁至 `.kimi-base/`+`.kimi-code/`（源布局=安装布局）；引擎 4712 行单文件拆为薄入口+lib/ 22 模块（无环显式 import，RISKS 前向引用已显式化）；installer 复制面重写（受管恒等映射+四种子语义，新增种子回归测试）；全路径重接线（plugin/package/ci/setup/tests/README）；根新增本仓宪法 AGENTS.md。证据：selftest 15/15、`node --test` 38/38、catalog lint / manifest --check / doctor / pack-check / arch check 全 exit 0（2026-09-02，Windows + Node 24 本机亲跑）；另修 selftest 两处 TMPDIR 硬编码为 os.tmpdir()（Windows 修复，原单文件时代遗留）。
- 六仓深度调研与 Kimi 官方文档核对完成。证据：docs/CROSS-POLLINATION.md、docs/CAPABILITY-MATRIX.md（2026-08-13）
- P2 架构定型：双层架构 + CLI 契约 + 五性模型。证据：Product-Spec.md REQ-001~030、docs/ARCHITECTURE.md（2026-08-13）
- P3 runtime 引擎：runtime/kimi-base.mjs（4712 行，23 动词+7 hook 事件）+ supervisor.mjs（448 行）+ 跨平台安装脚本。证据：`selftest` 15/15（2026-08-13）
- P4 模板资产：8 agents + 16 skills + 3 rules + 9 templates（template/，description lint 全过）
- P5 测试与插件胶水：tests/harness.test.mjs 37 用例 + plugin/skills + plugin/commands×8。证据：`node --test tests/harness.test.mjs` 37/37 通过（2026-08-13，Node 24）
- P6 集成验证（全部亲跑）：doctor/manifest --check/pack-check/selftest 全 exit 0；/tmp 端到端：缺证据完成门 exit 2（列 6 项缺口）→ gate→quality→complete 放行；改动后旧回执 stale 再拦；危险命令（wrapper 穿透/凭据跨管道外泄）exit 2、非标记项目静默 exit 0；arch 禁边检出→baseline 固化→放行；fast mode 2 条 SKIPPED 且 security 不跳过；security waiver 拒绝创建；Stop 保险丝实测 [2,2,2,0]；receipt verify 账本链完整
- P7 契约对齐：rules/skills/PROTOCOLS/ADR 全部对齐引擎真实契约（消灭 receipt write、arch record、quality attributes 等幽灵引用）；module-catalog 示例 layers 修正为最内层在前

## Risks & Assumptions

- Risk：Kimi hooks fail-open，脚本异常即放行 → Mitigation：分类器自身最小化+防御性编码；高危面叠加 permission rules；文档明示边界（docs/ISOLATION-PROFILES.md）。
- Risk：插件 commands/skills 全局可见造成他项目噪音 → Mitigation：sessionStart skill 首行即标记检查并静默；测试锁定惰性行为。
- Assumption：`subagents: []` 空列表语义为"禁止再派发"（Confidence: 高；依据官方文档"Allowlist…Omit to allow every type"的反向语义；待实装验证 #9）。
- Assumption：PreToolUse 的 tool_input 字段名与 Write/Edit 的 path 字段稳定（Confidence: 高；依据官方 hooks 文档示例）。

## Notes

- 家族供体仓库均在 /home/z00632348/code/ 下可读，移植以实际文件为准而非 README 宣传。
