# Product Spec — <产品名>

<!--
填写指引：
- 本文件是需求的唯一权威来源；任何变更必须同轮更新 Product-Spec-CHANGELOG.md（成对更新铁律）。
- 功能需求逐条编 REQ 号（REQ-001、REQ-002……），只增不复用；废弃条目标「已移除」不删号。
  下游 dev-planner / code-review / test-builder 以 REQ 号为锚。
- REQ 块按 spec lint 口径写：带规范词（必须/不得/应当）、带触发条件（当/若/WHEN/IF）、带「验收：」行。
  一个块 = REQ 行起 14 行（遇下一个 REQ 号截断）。
- 勿在 REQ 块内写「状态：planned」字样——那是 spec lint 的需求生命周期保留语法（必须带 phase 编号，
  形如 planned(P3)）；误写会被判 PLANNED_NO_PHASE，或被 trace 排除出覆盖率分母。
- 认知标注四态（spec lint 机械校验标签形态）：[确认] 带来源；[推断] 必须带「依据：」；
  [建议] 带理由；[未知] 必须带确认途径。未标条款按推断论处。集中写进第 3 节「现状与假设」。
- 每条需求必须可判定：写「用户做什么 → 系统做什么 → 得到什么」，不写形容词。
- 非功能需求可度量的写指标（数字+单位），不可度量的写边界（「不收集 XX」）。
- 非目标写清楚本期不做什么——它是防 scope creep 的墙。
- 全文禁止占位词；写不出来的条目用 [未知] + 确认途径表达，不留空壳。
- 本文件必须经用户明确批准（签字闸）后才可进 dev-planner；直推档小改动（改文案/加选项）豁免本闸，
  改完一句话复述即可，见 .kimi-base/rules/workflow.md 交互深度四档。
-->

_版本：v1.0_
_日期：<YYYY-MM-DD>_
_状态：草案 | 已批准_

## 1. 产品概述

<一段话说清楚：这是什么产品、解决什么问题、目标用户是谁（具体描述，不要只说「用户」）、核心价值是什么。>

## 2. 目标用户与场景

<3-5 个具体场景：谁、在什么情况下、怎么用、解决什么问题。>

## 3. 现状与假设（认知标注）

四态标签：`[确认]` 用户确认的事实（带来源）｜`[推断]` AI 推断（必须带「依据：」）｜`[建议]` AI 提议（带理由，不冒充用户需求）｜`[未知]` 未知项（必须带确认途径：问谁/做什么验证、何时答）。未标条款按推断论处。as-is 现状故事、受益受损者、决策历史的结论都落本节。

- [确认] <现状事实>（来源：<用户复盘的真实案例 / 对话日期>）
- [推断] <AI 的推断>（依据：<观察到的现象>）
- [建议] <AI 的提议>（理由：<为什么值得做>）
- [未知] <答不出的问题>（确认途径：<问谁、何时答；或「押后：不答先按 X 做」>）

## 4. 功能需求（REQ 编号）

### 核心功能

- **REQ-001** <功能名>：当<触发条件>时，用户<做什么> → 系统必须<做什么> → 得到<什么> ｜ 动机：<为什么做，来自哪条现状痛点> ｜ 受益人：<谁的日子变好>
  验收：<可判定的验收条件>
- **REQ-002** …

### 辅助功能

- **REQ-101** <功能名>：<同上格式：触发条件 → 行为 → 结果 ｜ 动机 ｜ 受益人>
  验收：<…>

## 5. 规则与例外

每条显式规则至少配一个例外；例外比规则更暴露真实逻辑。下游（code-review / bug-fixer / test-builder）回流的需求反例也进本节并标来源。

| 规则 | 例外 | 例外触发条件 | 来源 |
|---|---|---|---|
| <如：工单由组长统一派> | <如：VIP 客户直达老张> | <如：客户等级=VIP> | <访谈日期 / 下游反例> |

## 6. 用户流程

<分步骤描述主路径（从打开到完成任务）。受益者 ≠ 买单者时两条流程都要写，一人一条；例外路径单独写。>

## 7. 非功能需求（五性）

| 编号 | 维度 | 需求 | 度量/边界 |
|---|---|---|---|
| NFR-001 | 韧性 Resilience | <如：上游故障时核心流程可降级继续> | <如：MTTR ≤ 5min；或边界描述> |
| NFR-002 | 安全 Security | | |
| NFR-003 | 功能安全 Safety | | |
| NFR-004 | 隐私 Privacy | <如：收集哪些个人数据、存哪、留多久> | <如：日志零 PII> |
| NFR-005 | 可靠性 Reliability | | <如：崩溃率 / RPO / RTO> |

<不适用的维度写明「N/A + 理由」，不留白。>

## 8. 技术约束

| 维度 | 选择 | 理由 |
|---|---|---|
| 产品类型 | <Web / Desktop / CLI / Mobile> | |
| 技术栈方向 | | |
| 数据存储 | <本地 / 云端 / 混合> | |
| 部署方式 | | |
| 硬性限制 | <合规、预算、既有系统依赖等> | |

## 9. UI 布局（如适用）

<整体布局结构 + 各区域内容 + 控件规范。无 UI 则写 N/A。>

## 10. 明确非目标

<本期不做什么，逐条写清。例：「不做多租户」「不做移动端」「不做 XX 集成」。>

## 11. 成功标准

<怎么算做成了：可观察、可判定的标准。例：「核心流程从进入到完成 ≤ 3 步」「REQ-001~005 全部验收通过」。>

## 12. 验收边界

<验收覆盖到哪、不覆盖到哪。例：「验收以 Web 端 Chrome 为准」「性能压测不在本期验收范围」。>

## 13. 澄清记录

问过的不再问：每次问答落一行；下游阶段发现与既有答案矛盾的新事实时，先摆出矛盾再重问。用户纠正按三段式记录（旧理解 → 新理解 → 影响面）。

| 日期 | 问 | 答 | 落点（REQ/节） |
|---|---|---|---|
| <YYYY-MM-DD> | <问题> | <答案要点> | <REQ-00X / 第 N 节> |

纠正三段式：
- <YYYY-MM-DD>：原以为 <A>（当时依据：<…>）→ 实际 <B>（用户原话）→ 影响面：<改了哪些 REQ / 流程 / 已确认结论>
