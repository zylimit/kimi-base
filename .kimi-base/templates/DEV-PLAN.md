# Development Plan — <项目名>

<!--
填写指引：
- 每个 Phase 是一个可独立验收的功能单元，含 2-5 个 Task。
- 每个 Task 必须写齐五要素：Goal / Scope（精确到文件路径）/ Dependencies / Verification（具体命令）/ Expected（可判定结果）。
- 无占位符：禁 TBD、TODO、「待补充」「类似 Task N」「添加适当的错误处理」。
  执行者是没有项目上下文的 fresh 子代理——每个 Task 必须自足到照字面就能开工。
- Phase 排序 = 依赖 DAG 拓扑序：基础设施在前，被依赖方先做；无强依赖按用户价值洋葱剥皮；高风险前置。
- 末尾的 REQ → Phase 映射表必须覆盖 Spec 全部 REQ，漏一条就是漏需求。
- 完成的 Phase 标 ✅；迭代时已完成 Phase 不动。
-->

> 本文件记录项目的开发阶段划分、当前进度和剩余工作。
> 新 session 启动时应首先阅读此文件，了解项目状态后再继续开发。

## Phase 1: <功能名称>

**交付内容**：
- <用动词开头，描述交付物——用户能做什么 / 系统做什么>

**关键文件**：
- `<路径>` — <用途说明>

**Task 清单**：

- **Task 1.1：<任务名称>**
  - Goal：<明确实现什么行为>
  - Scope：`<文件路径1>`、`<文件路径2>`
  - Dependencies：<无 / 必须在 Task X.Y 之后，因为……>
  - Verification：`<具体命令>`
  - Expected：<可判定结果：退出码 0 / 输出含 <关键文本> / <可观察行为>>
- **Task 1.2：…**

**验收标准**：
- <最低：能编译、能启动、新功能可用；推荐加：现有功能未破坏>

---

## Phase 2: <功能名称>

<按 Phase 1 结构重复>

---

## 技术栈

| 层级 | 技术 | 版本 | 说明 |
|---|---|---|---|
| | | <经核实的版本号> | |

## 数据库表（如有）

| 表名 | 所属 Phase | 用途 |
|---|---|---|
| | | |

## REQ → Phase 映射

| REQ | 标题 | Phase |
|---|---|---|
| REQ-001 | | Phase N |

## 开发规则

- 每完成一个 Phase 执行四步走：Code Review → 测试完整性 → 编译验证 → 功能测试
- 四步走全部通过、用户确认后才能进下一 Phase
- Commit message 格式：`phase-N: 简要描述` / `fix: 简要描述`
