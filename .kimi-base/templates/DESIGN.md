# DESIGN.md —— <产品名> 设计规格与美学承诺

<!--
填写指引：
- 本文件是 design-maker 降级路径（无设计工具 MCP）的产物，也是设计稿的文字化锚点；
  Product-Spec.md 定功能范围，Design-Brief.md 定方向，本文件定可执行的规格。冲突裁决：设计稿 > Brief > Spec > 继承先例。
- 手动模式：无设计工具时本文件即设计交付物，在「模式」行如实标注；规格文字不等于已验证设计稿，不假绿。
- 第 0 节美学承诺必须先于任何具体样式值回答——先承诺后写 CSS，不是写完再补。
- 每个 token 必须带 $description 写清「用在哪、不得用在哪」（陷阱测试实证：无语义的 token 会被
  模型用到删除按钮上）。
- 全文不留占位词；定不下来的用「[未知] + 确认途径」表达，与 Product-Spec 认知标注同口径。
- 内容用真实业务文案与字段，禁止 Lorem ipsum 式假文。
-->

_版本：v1.0_
_日期：<YYYY-MM-DD>_
_模式：设计工具（<工具名>）| 手动模式（降级原因：<哪个工具不可用>）_

## 0. 美学承诺（commit-first：写任何样式值之前先回答）

- **目的**：<这个界面为谁解决什么问题；第一眼要把用户带去做什么。>
- **基调**：<三个词，每个词落到属性。例：冷静 = 中性色 + 数据优先排版 + 零装饰。>
- **约束**：<平台与形态、信息密度、可访问性底线（WCAG 级别、reduced-motion）、必须沿用的品牌资产。>
- **差异化**：<与最像的一个参照产品拉开距离的那一个决策——「和 Linear 比，我们在 X 上刻意不同」。>
- **一个可辩护的审美冒险**：<冒险是什么>；辩护：<为什么值得>；退路：<效果不成立时退回哪个安全方案>。
- **禁令清单**（每条禁令必须给命名替代——实证：禁了 Inter 不给替代，模型会收敛到 Space Grotesk 这类下一个烂大街默认；禁令不给替代 = 把选择权交回给模型的从众本能）：

| 禁止 | 命名替代 | 理由 |
|---|---|---|
| <禁：Inter 及同类默认无衬线> | <替代：具体命名的字体，如 IBM Plex Sans> | <为什么禁、为什么选这个替代> |
| <禁：某配色/动效/组件模式> | <替代方案> | <理由> |

## 1. 设计 tokens（每个 token 带 $description 语义）

```json
{
  "color": {
    "background": { "$value": "<色值>", "$description": "仅页面底色；不得用于卡片、文本或状态表达" },
    "surface": { "$value": "<色值>", "$description": "卡片、面板、弹窗；不得用于页面底色" },
    "action-primary": { "$value": "<色值>", "$description": "仅主行动按钮与选中态；不得用于正文、装饰、链接默认色或危险操作" },
    "danger": { "$value": "<色值>", "$description": "仅错误提示与危险操作；不得用于强调、品牌装饰或普通警示" },
    "text-primary": { "$value": "<色值>", "$description": "主文本；不得用于背景或装饰元素" },
    "text-secondary": { "$value": "<色值>", "$description": "次级说明文本；不得用于正文标题" }
  },
  "space": {
    "sm": { "$value": "<值>", "$description": "表单或按钮内部间距；不得用于页面区块分隔" },
    "lg": { "$value": "<值>", "$description": "页面区块间距；不得用于组件内部" }
  },
  "radius": {
    "md": { "$value": "<值>", "$description": "卡片与输入框圆角；终端形态此项置为 N/A 并在 description 说明" }
  },
  "motion": {
    "feedback": { "$value": "<时长与缓动>", "$description": "仅操作反馈；不得用于装饰性循环动画；reduced-motion 时降为即时切换" }
  }
}
```

## 2. 状态覆盖清单（逐页机械核对）

触发规则：有数据展示 → 空态；有异步 → 加载态；有可能失败 → 错误态。命中即必须设计，缺一即不完整。

| 页面 | 默认 | 空态 | 加载 | 错误 | 成功反馈 | 禁用/无权限 |
|---|---|---|---|---|---|---|
| <页面名> | ✓ | <有数据展示则必填> | <有异步则必填> | <有可能失败则必填> | <有操作回路则必填> | <有权限门槛则必填> |

Agent 形态追加运行五态覆盖：流式输出中 / 思考中 / 跑工具中（须显示在跑什么）/ 等授权 / 被中断——每态一行写呈现方案，共用一个 spinner 视为未覆盖；长任务另写进度呈现。

## 3. 页面规格

### <页面名>（关联 <FLOW / REQ 号>）

- **页面目的**：<帮助用户完成什么。>
- **主要操作**：<最重要的一个操作；次要操作列在下。>
- **布局分区**：<顶部 / 主内容区 / 辅助区 / 固定操作区各放什么；工作台型先标不可压缩的主任务区域。>
- **内容层级**：<第一眼看到什么 → 其次 → 辅助信息。>
- **使用的组件**：<组件名 + 用途，来自第 4 节清单。>
- **状态变体**：<本页命中的状态及各自呈现方案。>
- **响应式 / 列宽降级**：<界面型写移动/平板/桌面规则；终端形态写 80/120 列怎么塌。>
- **真实内容样例**：<取自 Spec 业务上下文的真实文案与字段，不写假文。>

## 4. 组件清单

| 组件 | 用途 | 变体（主/次/危险…） | 状态（默认/悬停/聚焦/禁用/加载/错误） | 使用位置 |
|---|---|---|---|---|
| <组件名> | <解决什么问题> | <变体> | <状态方向> | <页面名> |

## 5. 交接与边界

- **给 dev-builder**：tokens 的 $description 是语义边界——把强调色用到删除按钮上即越界；页面规格的「真实内容样例」即文案基线。
- **回流口**：规格定不了的方向回 design-brief-builder；Spec 没覆盖的功能回 product-spec-builder；本文件不发明需求。
- **手动模式边界**：本文件是规格不是稿；凡未经截图验证的视觉效果，实现后须逐组件截图回验并回填结论。
